# Qadi — Specification

> **Document Control**
>
> | Property       | Value                                          |
> | -------------- | ---------------------------------------------- |
> | Document ID    | QADI-00                                        |
> | Revision       | 1.48                                           |
> | Effective Date | 2026-07-25                                     |
> | Status         | Effective                                      |
> | Author         | Qadi Engineering                               |
> | Classification | Functional Specification — Master Index        |
> | Change History | 1.48 (2026-08-24): The transport — a buffering feed and a guarded SSE route; behaviour 26, BEH-QD-201, BEH-QD-202, ADR-QD-046 (CCR-QD-065)<br>1.47 (2026-08-24): The topology is a choice of sink; BEH-QD-187, BEH-QD-188, ADR-QD-045 (CCR-QD-064)<br>1.46 (2026-08-24): The record's wire form; BEH-QD-199, BEH-QD-200 (CCR-QD-063)<br>1.45 (2026-08-24): The remaining devtools gaps resolved in code rather than deferred; BEH-QD-195–198, `/__permissions` guarded by default, five package READMEs and npm metadata (CCR-QD-062)<br>1.44 (2026-08-24): Six questions the library could pose and not answer, resolved in code rather than recorded as gaps; behaviour 25, BEH-QD-189–194, INV-QD-037, INV-QD-038 (CCR-QD-061)<br>1.43 (2026-08-23): The decision sink — the first channel by which a decision can be observed at all; behaviour 24, BEH-QD-181–186, INV-QD-035, INV-QD-036, ADR-QD-044, and ADR-QD-012 amended (CCR-QD-060). Four drifts in this document's own contents tables corrected, none of them gated<br>1.42 (2026-08-23): The HTTP boundary made fail-closed, and behaviour 23 written — the document `@qadi/http` shipped without; BEH-QD-174–180, INV-QD-034, ADR-QD-036 Rev 1.3 (CCR-QD-059)<br>1.41 (2026-08-23): Two fail-opens found by auditing @qadi/http and fixed in @qadi/core — `guard` evaluated without the resource, and the cache keyed on the subject's id; INV-QD-032, INV-QD-033, ADR-QD-043, BEH-QD-055, BEH-QD-168; behaviours 07 Rev 1.3 and 21 Rev 1.3. CI had been red on every clone since @qadi/http landed (CCR-QD-058). The ADR count corrected from forty-two to forty-three<br>1.40 (2026-08-23): Production-readiness sweep — publish access, `"use client"` with the first server-rendering test, unambiguous explanation prose, a cache key that cannot collide, and a payload that says what it dropped; INV-QD-030, INV-QD-031, ADR-QD-042, BEH-QD-167; behaviours 06/09/18/19/21 revised (CCR-QD-057). The ADR count corrected from forty-one to forty-two<br>1.39 (2026-08-23): An unwired port names its own absence; behaviour 06 Rev 1.3, BEH-QD-045, INV-QD-029, ADR-QD-040 (CCR-QD-055). A superseded seed is announced; behaviour 19 Rev 1.2, behaviour 09 Rev 2.3, BEH-QD-152, ADR-QD-041 (CCR-QD-056). The ADR count corrected from thirty-nine to forty-one<br>1.38 (2026-08-23): `Can`/`Cannot` hand their denial to the node replacing it; behaviour 09 Rev 2.2, BEH-QD-072 (CCR-QD-054)<br>1.37 (2026-08-23): `renderTrace`, and the trace on `AccessDenied`; behaviour 07 Rev 1.2, behaviour 18 Rev 1.1, BEH-QD-054, BEH-QD-144 (CCR-QD-053)<br>1.36 (2026-08-23): A hydrated allow no longer outlives the client's own denial — a client-side authorization bypass; behaviour 19 Rev 1.1, INV-QD-028, ADR-QD-039 (CCR-QD-052). CCR-QD-041–051 backfilled into the Document history register, which had recorded nothing since CCR-QD-040 while eleven were in use elsewhere; the ADR count corrected from thirty-four to thirty-nine<br>1.35 (2026-07-27): The dispatch benchmark; ADR-QD-034, and two switches found silently non-exhaustive (CCR-QD-040)<br>1.34 (2026-07-27): AGENTS.md §5a made enforceable; it named two switch exceptions where there were four (CCR-QD-039)<br>1.33 (2026-07-26): The packed artifact made a merge gate; two publish defects found (CCR-QD-038)<br>1.32 (2026-07-26): Mutation survivors read; one real gap found and closed (CCR-QD-037)<br>1.31 (2026-07-26): CI added, running `pnpm check` and nothing else (CCR-QD-036)<br>1.30 (2026-07-26): Repo-wide drift sweep — six false "in CI" claims, the root README's Phase 0 banner, four stale traceability ranges (CCR-QD-035)<br>1.29 (2026-07-26): Contents tables resynchronised — six behaviour documents and eight ADRs were absent, and two behaviour ranges were stale (CCR-QD-035)<br>1.28 (2026-07-26): API-surface drift made a merge gate; `spec/overview.md` Rev 1.2 (CCR-QD-034)<br>1.27 (2026-07-26): The Promise facade; behaviour 22, INV-QD-026, ADR-QD-032 (CCR-QD-033)<br>1.26 (2026-07-26): The decision cache; behaviour 21, INV-QD-025, ADR-QD-031 (CCR-QD-032)<br>1.25 (2026-07-26): Policy simplification; behaviour 20, INV-QD-024, ADR-QD-030 (CCR-QD-031)<br>1.24 (2026-07-26): `join` and `meet`; INV-QD-023, ADR-QD-029; MLS to Shipped (CCR-QD-030)<br>1.23 (2026-07-26): Decision hydration; behaviour 19, INV-QD-022, ADR-QD-028 (CCR-QD-029)<br>1.22 (2026-07-26): Policy explanation; behaviour 18, INV-QD-021, ADR-QD-027 (CCR-QD-028)<br>1.21 (2026-07-26): Concurrent evaluation; behaviour 17, INV-QD-020, ADR-QD-026 (CCR-QD-027)<br>1.20 (2026-07-26): Mutation testing as a merge gate; ADR-QD-025 (CCR-QD-026)<br>1.19 (2026-07-26): `spec/overview.md` and BEH-QD-042 brought up to date — the public API surface and the service count (CCR-QD-025)<br>1.18 (2026-07-26): Bell–LaPadula and MLS statuses earned; INV-QD-019 and BEH-QD-102, the order laws (CCR-QD-024)<br>1.17 (2026-07-26): Biba verified as `@REQ-QD-020`; MOD-QD-028 corrected — the low-water-mark E5 forecast withdrawn and the shadowing hazard recorded; the roadmap gate counts brought up to date (CCR-QD-023)<br>1.16 (2026-07-26): Chinese Wall and task-based control verified; MOD-QD-030/031/033 corrected (CCR-QD-022)<br>1.15 (2026-07-26): Behaviours 15 and 16; separation of duty verified; CCR-QD-018–020 backfilled (CCR-QD-021)<br>1.14 (2026-07-26): Subject sets built; behaviour 14 and INV-QD-016 added (CCR-QD-018)<br>1.13 (2026-07-26): Label lattice built; behaviour 13 and INV-QD-015 added (CCR-QD-017)<br>1.12 (2026-07-26): Decision history built; behaviour 12 and INV-QD-014 added; INV-QD-008 restated (CCR-QD-016)<br>1.11 (2026-07-26): Obligations built; behaviour 11, INV-QD-012 and INV-QD-013 added (CCR-QD-015)<br>1.10 (2026-07-26): ADR-QD-019, obligations (CCR-QD-014)<br>1.9 (2026-07-26): Reactivity canary; BEH-QD-071 corrected (CCR-QD-013)<br>1.8 (2026-07-26): Action dimension built; behaviour 10 and INV-QD-011 added (CCR-QD-012)<br>1.7 (2026-07-26): ADR-QD-018, the action dimension (CCR-QD-011)<br>1.6 (2026-07-26): Span emission verified (CCR-QD-010)<br>1.5 (2026-07-26): Relationship short-circuit coverage closed (CCR-QD-009)<br>1.4 (2026-07-26): Core-change and excluded model documents (CCR-QD-008)<br>1.3 (2026-07-26): Wiring-only model documents (CCR-QD-007)<br>1.2 (2026-07-26): Shipped-model documents (CCR-QD-006)<br>1.1 (2026-07-26): Models index; renamed to Qadi (CCR-QD-004, CCR-QD-005)<br>1.0 (2026-07-25): Initial release (CCR-QD-001) |

---

This specification is **normative**. Code follows it; where they disagree, one
of them is a defect.

Every document is mechanically checked. `spec/scripts/verify-traceability.sh`
verifies that the registries match the files on disk, that every invariant and
decision is traced, that every acceptance tag is defined, and that no relative
link is broken. `scripts/check-doc-examples.mjs` compiles the runnable examples.

## Contents

### Foundations

| Document | Purpose |
| -------- | ------- |
| [Overview](./overview.md) | Mission, design philosophy, public API surface |
| [User Requirements](./urs.md) | What users need, in their terms — and the two gaps writing it surfaced, both now closed |
| [Glossary](./glossary.md) | Terms of art, and where this codebase narrows an industry sense |
| [Invariants](./invariants.md) | Properties that hold for every execution, and what enforces each |
| [Traceability](./traceability.md) | Behavior → source → test → invariant → decision → scenario |
| [Roadmap](./roadmap.md) | What is deliberately unbuilt, and why |

### Models

| Document | Purpose |
| -------- | ------- |
| [00 — Access Control Model Adoption Matrix](./models/00-adoption-matrix.md) | Which models Qadi expresses, which it does not, and what each would cost |
| [01 — Role-Based Access Control](./models/01-rbac.md) | RBAC₀ and RBAC₁; why RBAC₂ needs an enabler |
| [02 — Attribute-Based Access Control](./models/02-abac.md) | Subject and resource attributes; lazy resolution |
| [03 — Relationship-Based Access Control](./models/03-rebac.md) | The relationship port, and why Qadi ships no graph store |
| [04 — Capability and Permission Tokens](./models/04-capability.md) | Permission tokens; why this is not object-capability |
| [05 — Identity-Based Access Control](./models/05-ibac.md) | `subjectId()` ownership, and when to prefer ReBAC |
| [06 — Content-Dependent Access Control](./models/06-content-dependent.md) | Deciding on the data's own values; the row-level boundary |
| [07 — Field-Level Authorization](./models/07-field-level.md) | The visibility lattice — the reason this library exists |
| [08 — Discretionary Access Control](./models/08-dac.md) | Owner-granted access; why granting itself is out of scope |
| [09 — Access Control Lists](./models/09-acl.md) | ACL entries as relation tuples; deny rows need an enabler |
| [10 — Zanzibar-Style Relationship Stores](./models/10-zanzibar.md) | SpiceDB / OpenFGA adapters; Qadi is the policy side |
| [11 — Claims-Based Access Control](./models/11-claims.md) | Tokens to subjects; scopes are not permissions |
| [12 — Context-Aware Access Control](./models/12-context-aware.md) | Device, network and posture; failure is not denial |
| [13 — Temporal Access Control](./models/13-temporal.md) | Time from `Clock`, never the ambient clock |
| [14 — Spatial Access Control](./models/14-spatial.md) | Geofencing and data residency; geometry stays in the resolver |
| [15 — Risk-Adaptive Access Control](./models/15-risk-adaptive.md) | Score in the resolver, threshold in the policy |
| [16 — Trust- and Reputation-Based Access Control](./models/16-trust.md) | Standing rather than assignment; an adversarial input |
| [17 — Purpose-Based Access Control](./models/17-purpose.md) | GDPR purpose limitation as a field projection |
| [18 — Consent-Based Access Control](./models/18-consent.md) | A three-party relation; revocation for free |
| [19 — Hierarchical Resource Scoping](./models/19-hierarchy.md) | Tenant and folder trees; `depth` and its limits |
| [20 — Team-Based Access Control](./models/20-tmac.md) | Care teams and case teams; role ∧ team |
| [21 — Organisation-Based Access Control](./models/21-orbac.md) | Multi-tenant rule catalogues; activity has no home yet |
| [22 — Type Enforcement](./models/22-type-enforcement.md) | SELinux-shaped domain/type matrices, and why it is not MAC |
| [23 — Label-Based Access Control](./models/23-label-based.md) | Clearances and classifications; dominance needs an enabler |
| [24 — Separation of Duty](./models/24-separation-of-duty.md) | Static, dynamic and object-based SoD; the last already works |
| [25 — Rule-Based Access Control](./models/25-rubac.md) | Ordered first-match rule lists; needs combining algorithms |
| [26 — XACML Parity](./models/26-xacml.md) | What parity would take, and why not to chase completeness |
| [27 — Bell–LaPadula](./models/27-bell-lapadula.md) | No read up, no write down; the guarantee is transitivity |
| [28 — Biba](./models/28-biba.md) | The integrity dual; a water mark is the caller's to keep |
| [29 — Multi-Level Security](./models/29-mls.md) | The Denning lattice, order and algebra both |
| [30 — Chinese Wall](./models/30-chinese-wall.md) | Conflict of interest; the wall is built by the first access |
| [31 — History-Based Access Control](./models/31-hbac.md) | Rate limits and quotas; the port must not become a database |
| [32 — Usage Control](./models/32-ucon.md) | UCON's ABC model; continuity is a deliberate non-goal |
| [33 — Task-Based Access Control](./models/33-tbac.md) | Workflow authorizations; the once-ness was one conjunct |
| [34 — Next Generation Access Control](./models/34-ngac.md) | Policy as a graph; treat it as a resolver, not a rewrite |
| [35 — Row-Level Security](./models/35-row-level.md) | Predicates instead of decisions; the largest departure |
| [36 — Cell-Level Security](./models/36-cell-level.md) | Per-value visibility; more of it ships than expected |
| [37 — Models Qadi Does Not Implement](./models/37-excluded.md) | The boundary, and what pairs with Qadi instead |

Model documents are planning records, not specification. They carry `MOD-QD-NNN`
identifiers precisely so that describing a capability cannot be mistaken for
having verified one.

### Behaviors

| Document | Requirements |
| -------- | ------------ |
| [01 — Permission Tokens](./behaviors/01-permissions.md) | BEH-QD-001–006 |
| [02 — Roles and Inheritance](./behaviors/02-roles.md) | BEH-QD-009–012 |
| [03 — Policy ADT](./behaviors/03-policy-adt.md) | BEH-QD-017–020 |
| [04 — Matcher DSL](./behaviors/04-matchers.md) | BEH-QD-025–028 |
| [05 — Evaluator](./behaviors/05-evaluator.md) | BEH-QD-033–040 |
| [06 — Services and Layers](./behaviors/06-services.md) | BEH-QD-041–044 |
| [07 — Enforcement](./behaviors/07-enforcement.md) | BEH-QD-049–054 |
| [08 — Serialization](./behaviors/08-serialization.md) | BEH-QD-057–059 |
| [09 — React Integration](./behaviors/09-react.md) | BEH-QD-065–072 |
| [10 — The Action Dimension](./behaviors/10-actions.md) | BEH-QD-073–078 |
| [11 — Obligations](./behaviors/11-obligations.md) | BEH-QD-081–087 |
| [12 — Decision History](./behaviors/12-history.md) | BEH-QD-089–095 |
| [13 — The Label Lattice](./behaviors/13-labels.md) | BEH-QD-097–104 |
| [14 — Subject Sets](./behaviors/14-subject-sets.md) | BEH-QD-105–109 |
| [15 — Rule Tables](./behaviors/15-rules.md) | BEH-QD-111–117 |
| [16 — Predicate Output](./behaviors/16-predicates.md) | BEH-QD-121–128 |
| [17 — Concurrent Evaluation](./behaviors/17-concurrency.md) | BEH-QD-129–135 |
| [18 — Policy Explanation](./behaviors/18-explanation.md) | BEH-QD-137–144 |
| [19 — Decision Hydration](./behaviors/19-hydration.md) | BEH-QD-145–152 |
| [20 — Policy Simplification](./behaviors/20-simplification.md) | BEH-QD-153–156 |
| [21 — Decision Cache](./behaviors/21-decision-cache.md) | BEH-QD-161–168 |
| [22 — The Promise Facade](./behaviors/22-promise-facade.md) | BEH-QD-169–173 |
| [23 — HTTP Enforcement](./behaviors/23-http.md) | BEH-QD-174–180 |
| [24 — The Decision Sink](./behaviors/24-decision-sink.md) | BEH-QD-181–188 |
| [25 — Inspection](./behaviors/25-inspection.md) | BEH-QD-189–200 |
| [26 — The Decision Stream](./behaviors/26-decision-stream.md) | BEH-QD-201–202 |

### Decisions

Forty-six ADRs, [indexed here](./decisions/index.yaml). The load-bearing ones:

| ADR | Decision |
| --- | -------- |
| [ADR-QD-002](./decisions/002-schema-derived-policy-adt.md) | The policy ADT is schema-derived — the central decision |
| [ADR-QD-004](./decisions/004-single-effect-evaluator.md) | One `Effect`-returning evaluator |
| [ADR-QD-005](./decisions/005-lazy-attribute-resolution.md) | Lazy per-node attribute resolution |
| [ADR-QD-011](./decisions/011-enforce-as-aspect.md) | `Qadi.enforce` is an Effect aspect |
| [ADR-QD-014](./decisions/014-react-via-atoms.md) | React integrates through Effect atoms |
| [ADR-QD-016](./decisions/016-gxp-out-of-scope.md) | GxP compliance is out of scope |
| [ADR-QD-018](./decisions/018-action-dimension.md) | The action is an evaluation input, not a permission segment |
| [ADR-QD-019](./decisions/019-obligations.md) | An obligation is a condition on permission |
| [ADR-QD-020](./decisions/020-decision-history-port.md) | History is a three-valued port — a boolean cannot fail closed under negation |
| [ADR-QD-021](./decisions/021-label-lattice.md) | Dominance is four-valued; the label never enters the policy |
| [ADR-QD-022](./decisions/022-subject-set-evaluation.md) | A subject set is asked by nobody, and reports rather than enforces |
| [ADR-QD-023](./decisions/023-combining-algorithms.md) | A rule list stops at the first rule that cannot be overridden |
| [ADR-QD-024](./decisions/024-predicate-output.md) | A predicate is a second interpreter, shipped with its reference semantics |
| [ADR-QD-025](./decisions/025-mutation-testing.md) | Mutation testing is a merge gate, and its evidence must be reproducible by someone else |
| [ADR-QD-026](./decisions/026-concurrent-evaluation.md) | Concurrency changes what is looked up, never what is decided |
| [ADR-QD-028](./decisions/028-decision-hydration.md) | A hydrated decision is bound to a subject and carries no trace |
| [ADR-QD-031](./decisions/031-decision-cache.md) | A cache stores the trace, and the key is the security boundary |
| [ADR-QD-032](./decisions/032-promise-facade.md) | A Promise facade with no evaluator in it |
| [ADR-QD-039](./decisions/039-a-seed-is-not-an-authority.md) | A seed is not an authority, so it lives in its own atom |

All forty-six are **Accepted** and describe code that exists. ADR-QD-018
through ADR-QD-024 were each written *Proposed* first — recording a
decision whose implementation had not landed — and moved to Accepted when the
capability shipped with its behaviour, invariant and scenario. That is the path a
Proposed decision takes, and for ADR-QD-020 and ADR-QD-021 it was mandatory: the
matrix required each of their design questions settled before any code — and
ADR-QD-022 was written the same way for a question the matrix said did not
exist. The
status is what tells the two apart, and nothing may cite a Proposed decision as
evidence of behaviour.

### Appendices

| Document | Purpose |
| -------- | ------- |
| [React Integration Guide](./appendices/react-integration.md) | A worked application, from wiring to testing — every example compiled by the merge gate |

### Process

| Document | Purpose |
| -------- | ------- |
| [Requirement Identifier Scheme](./process/requirement-id-scheme.md) | ID allocation and cross-reference obligations |
| [Definitions of Done](./process/definitions-of-done.md) | The merge gate |

### Reading order

New to the library: [Overview](./overview.md) → [User Requirements](./urs.md) →
[Glossary](./glossary.md) → the behaviors.

Reviewing a change: [Invariants](./invariants.md) →
[Traceability](./traceability.md) → [Definitions of Done](./process/definitions-of-done.md).

## Why this library exists

Qadi replaces an earlier `Result`-based authorization library. The rewrite was
prompted by defects that were structural rather than incidental — each came from
maintaining two representations of one thing and letting them drift:

| Defect | Cause | Now prevented by |
| ------ | ----- | ---------------- |
| Field visibility silently narrowed on a JSON round trip | Hand-written codec drifted from the type | [INV-QD-003](./invariants.md#inv-qd-003-codectype-identity) |
| Async relationship API never called | Two evaluators, one unreachable | [ADR-QD-004](./decisions/004-single-effect-evaluator.md) |
| Short-circuiting destroyed by eager resolution | Resolve-then-evaluate in two phases | [INV-QD-005](./invariants.md#inv-qd-005-short-circuit-preservation) |
| One error code for two unrelated failures | Manual code allocation | [INV-QD-010](./invariants.md#inv-qd-010-error-codes-are-injective) |
| Documentation examples that did not compile | Nothing checked them | `scripts/check-doc-examples.mjs` |

The first of those was reproduced against the original implementation before
this rewrite began: a policy exposing `["title", "author"]` returned only
`["title"]` after being stored and reloaded, with no error anywhere.

## Identifier scheme

| Prefix | Meaning |
| ------ | ------- |
| `BEH-QD-NNN` | Functional behavior requirement |
| `INV-QD-NNN` | Runtime invariant |
| `ADR-QD-NNN` | Architecture decision |
| `REQ-QD-NNN` | Acceptance scenario tag on a `.feature` file |
| `MOD-QD-NNN` | Access control model adoption record — planning, not specification |
| `CCR-QD-NNN` | Change control record |

Full rules in [the identifier scheme](./process/requirement-id-scheme.md).

## Document history

| CCR | Date | Change |
| --- | ---- | ------ |
| CCR-QD-001 | 2026-07-25 | Initial specification |
| CCR-QD-002 | 2026-07-25 | Glossary, user requirements and roadmap |
| CCR-QD-003 | 2026-07-26 | React rebuilt on `effect/unstable/reactivity` (ADR-QD-014 revised, ADR-QD-017 added) |
| CCR-QD-004 | 2026-07-26 | Access control model adoption matrix; `MOD-QD` identifier series registered |
| CCR-QD-005 | 2026-07-26 | Library renamed Guard → Qadi; scope `@guard/*` → `@qadi/*`; infix `EG` → `QD`; service tags, span names and document ids follow |
| CCR-QD-006 | 2026-07-26 | Model documents for the seven shipped access control models (MOD-QD-001–007) |
| CCR-QD-007 | 2026-07-26 | Model documents for the sixteen wiring-only models (MOD-QD-008–023) |
| CCR-QD-008 | 2026-07-26 | Model documents for the core-change and excluded models (MOD-QD-024–037); model set complete |
| CCR-QD-009 | 2026-07-26 | Relationship short-circuit and resolver-failure coverage; URS-QD-010 known gap closed |
| CCR-QD-010 | 2026-07-26 | Span emission verified with a collecting tracer; URS-QD-012 known gap closed |
| CCR-QD-011 | 2026-07-26 | ADR-QD-018 decides the action dimension (E1); first *Proposed* decision in the set |
| CCR-QD-012 | 2026-07-26 | Action dimension built: `hasAction`, `action()`, `MissingAction`, `qadi.action`; BEH-QD-073–078, INV-QD-011, URS-QD-015, `@REQ-QD-010`; ADR-QD-018 Accepted |
| CCR-QD-013 | 2026-07-26 | Canary over `effect/unstable/reactivity`; BEH-QD-071 corrected — atom keying is structural, not by reference; last release blocker closed |
| CCR-QD-014 | 2026-07-26 | ADR-QD-019 decides obligations (E2); corrects the model documents' `FieldStrategy` assumption and their "not a codec change" claim |
| CCR-QD-015 | 2026-07-26 | Obligations built: `obliged`, `Obligation`, `UndischargedObligation`, `onObligations`; BEH-QD-081–087, INV-QD-012, INV-QD-013, URS-QD-016, `@REQ-QD-011`; ADR-QD-019 Accepted |
| CCR-QD-016 | 2026-07-26 | Decision history built: `DecisionHistory`, `hasActed`, `hasNotActed`, `DecisionHistoryUnavailable`; BEH-QD-089–095, INV-QD-014, URS-QD-017, `@REQ-QD-012`; INV-QD-008 restated as "given the same history"; ADR-QD-020 Accepted |
| CCR-QD-017 | 2026-07-26 | Label lattice built: `SecurityLabel`, `compareLabels`, `dominates`; BEH-QD-097–101, INV-QD-015, URS-QD-018, `@REQ-QD-013`; ADR-QD-021 Accepted |
| CCR-QD-018 | 2026-07-26 | Subject sets built: `decideSubjects`, `filterSubjects`, `SubjectSetServices`, `qadiReviewLayer`; BEH-QD-105–109, INV-QD-016, URS-QD-019, `@REQ-QD-014`; ADR-QD-022 Accepted; phase 4 of the adoption matrix complete |
| CCR-QD-019 | 2026-07-26 | Combining algorithms built: `rules`, `permitWhen`, `denyWhen`, `Combining`, `RuleEffect`; BEH-QD-111–117, INV-QD-017, URS-QD-020, `@REQ-QD-015`; INV-QD-005 defers to INV-QD-017; ADR-QD-023 Accepted |
| CCR-QD-020 | 2026-07-26 | Predicate output built: `Predicate`, `toPredicate`, `evaluatePredicate`, `PolicyNotTranslatable`; BEH-QD-121–128, INV-QD-018, URS-QD-021, `@REQ-QD-016`; ADR-QD-024 Accepted; phase 5 complete, every enabler shipped |
| CCR-QD-021 | 2026-07-26 | Separation of duty verified as `@REQ-QD-017`; MOD-QD-024 corrected — static SoD to *Shipped, in part*, `DecisionHistoryAssumeActed` withdrawn, and two forecasts corrected (a label is a trace property, and an absent field grants) |
| CCR-QD-022 | 2026-07-26 | Chinese Wall verified as `@REQ-QD-018` and task-based control as `@REQ-QD-019`; MOD-QD-030, MOD-QD-031 and MOD-QD-033 corrected — all three to Shipped (HBAC in part, the count and the ordering question deferred), `DecisionHistorySealed` and 30's `Engagement` union withdrawn, 33's absent-`raisedBy` hazard closed in a compiled example; MOD-QD-032's E5 claims brought up to date |
| CCR-QD-023 | 2026-07-26 | Biba verified as `@REQ-QD-020`, both variants; MOD-QD-028 corrected to Rev 1.1 — its "low-water-mark needs E5" finding **withdrawn** (a water mark is an aggregate the port cannot supply; E1/E4 alone was right), the ADR it asked for shown to be ADR-QD-020, and the BEH-QD-034 shadowing hazard recorded as a failure that opens; `Shipped, in part` added to the matrix legend; the Bell–LaPadula and MLS status disagreement recorded as open; `spec/roadmap.md` gate counts corrected after two revisions of drift |
| CCR-QD-024 | 2026-07-26 | Bell–LaPadula and MLS statuses earned, closing the item CCR-QD-023 left open. MOD-QD-027 → Shipped on a **borrowed** `@REQ-QD-013`; MOD-QD-029 → Shipped, in part as a **ceiling**, because it defines a lattice as "a partial order with joins" and `join`/`meet` were declined — three of the seven laws in its Verification table were unsatisfiable rather than unmet. MLS verified as `@REQ-QD-021`, stating the rule as flow with no `hasAction`. Both documents had prescribed property tests for the order laws and neither got them: INV-QD-019 and BEH-QD-102 now assert reflexivity, antisymmetry and transitivity, and record that 27's "composite property" **is** transitivity — the ★-property's guarantee is a consequence of the order, not a rule the evaluator enforces. Five prior unbumped revisions recorded across the two documents |
| CCR-QD-055 | 2026-08-23 | **An unwired port names its own absence.** `hasRelationship` evaluated with nothing wired denied with `subject 'u1' has no 'owner' relation to 'doc-1'` — a claim about the contents of a graph that had never been connected. `RelationshipResolverShape.check` returned a `boolean`, so the evaluator could not tell "the store says no" from "there is no store". BEH-QD-045, INV-QD-029, ADR-QD-040 (extending ADR-QD-020); behaviour 06 Rev 1.3. `RelatedResult = "Related" | "Unrelated" | "Unknown"` mirrors `ActedResult` exactly. **The verdicts do not move** — both new arms deny where the boolean denied, INV-QD-007 is untouched — so this is an invariant about diagnosis, and nothing verdict-shaped could have caught its violation. Note what ADR-QD-020 had already seen and correctly declined to fix: it named this layer's `false` "the exact counterpart" of `"Unknown"`, then left the port boolean because `hasRelationship` has no negative polarity and `false` was fail-closed. That was right; the residue was the sentence. **Deliberately breaking, and the widening was rejected for being non-breaking**: `boolean | "Unknown"` keeps every implementation assignable and every `if (check(...))` compiling while `"Unknown"` is truthy — an unwired port silently reading as *related*, a grant introduced by a type change. `assert.isTrue` takes `unknown`, so seven test sites compiled against the new union and failed only at run time; replaced with typed helpers that cannot confuse `"Unrelated"` with `"Unknown"`. Also here, the milder sibling: `HasAttribute` said `did not match` for an absent attribute as well as a wrong one — true of `undefined`, so never false, but the case `AttributeResolverNone` produces exclusively; it now says `has no value`. Gate 8 found what this change's plan had assumed away: **eight `spec/models/*.md` resolvers** implement the port and every one failed to compile |
| CCR-QD-065 | 2026-08-24 | **The transport, on the seam CCR-QD-064 left.** Two questions remained and they are not independent — which protocol carries records, and what stops the wrong reader getting them — so the second was settled first, because it constrains the first. **`/__decisions` is guarded by a policy with no unguarded variant**, unlike `/__permissions`, and the asymmetry is the disclosure: a topology is a map, decisions are the traffic on it, including subject identifiers, verdicts, resources and whatever a `Trace` names about why. There is deliberately **no `NODE_ENV` gate** — an ambient value deciding who may read authorization data is precisely the inversion BEH-QD-174 rejects, and a deployment that wants this off does not mount it, which is a decision visible in its wiring rather than its environment. **SSE rather than a WebSocket**, decided by the traffic: records flow one way, and SSE is plain HTTP so the route passes through the same router, middleware and `guardRoute` as every other route — a socket's upgrade path sits outside all three and would have to re-answer authorization on its own terms, which is the second-path defect this library removes elsewhere. Effect's own devtools uses a WebSocket and is right to; it is a bidirectional RPC channel, this is a feed. **`decisionSinkFeed` is the buffering sink ADR-QD-045 deferred** — deferred then because building one against no transport would have been speculative, built now because there is one. A `PubSub.sliding` with `publishUnsafe` means publishing never blocks and never fails, whatever the reader is doing including when there is none, which is the only acceptable behaviour for something an authorization decision waits on. Sliding rather than dropping so a reconnecting reader gets the most recent decisions, matching the ring's eviction so a reader sees one policy rather than two. Recorded as a cost rather than hidden: SSE is one-way, so a devtools that later wants to *send* something needs a second channel. One process lesson worth keeping: the feed's tests were first written by forking a collector and publishing into it, which reads naturally and turned the **mutation gate from four minutes into three hours** — kill the publish and the forked fiber blocks until Stryker's timeout, once per mutant with coverage there. Rewritten to terminate on their own (`replay` for delivery, one `it.live` with a wall-clock timeout for the live-feed contract), the gate returned to four minutes and its timeout count to the pre-existing seventeen. A test suite that makes its own gate impractical is a defect in the suite |
| CCR-QD-064 | 2026-08-24 | **The topology is a choice of sink, and core ships only the seam.** `decisionSinkRing` answers "what did *this* process decide", and three of the six deployments Qadi runs in are not served by that: a replicated server has n rings and a reader reaches whichever instance answered its own request, a serverless function's ring dies with the invocation, and a browser talking to a separate API origin is two processes of which the deciding one has no page. The temptation here is to ship a transport — a WebSocket sink, an HTTP forwarder — and that is where an authorization library stops being one. `decisionSinkForwarding({send})` projects a record onto the wire and hands the encoded value to a caller-supplied function; which socket, store, framing and retry policy lie beyond it belong to the caller, and `@qadi/core` gains no dependency that could pull a transport in. This is the payoff for making the port **write-only** in CCR-QD-060: reading back was left to implementations precisely so the topology could be one. `decisionSinkAll` fans out, because the real shape is both a local ring and a forwarder — and merging two `Layer`s for one service does **not** do that, the later simply wins while the first silently sees nothing, which is asserted rather than described. `ingest` is the receiving half and takes the environment as a **parameter**: a merged log holds rows from several processes, and stamping them with the aggregator's own label would erase the distinction the merge exists to preserve. One bound and one eviction path serve both routes in, since an aggregator taking records from n replicas is where an unbounded log would hurt most. Two things are recorded rather than engineered around: `send` carries a contract the type cannot express — it must not block, because `record` is awaited inside the evaluation so records stay ordered under `TestClock` — and buffering inside the forwarder, which would remove that hazard rather than warn about it, is deferred as needing a scope, fiber supervision and a flush-on-shutdown story that would be speculative against no real transport. Nothing here yet exercises a real socket; the seam is proven against an in-memory `send` |
| CCR-QD-063 | 2026-08-24 | **The record's wire form** — the first task of the transport increment, and deliberately not the socket. An in-memory sink hands a consumer real objects; anything crossing a process boundary needs a form that survives JSON and rebuilds on the far side, and `SinkRecord` had no such form. `Policy` already had a codec and `Trace` is plain data, but `Failed.error` is a `Data.TaggedError` with none — and AGENTS.md §4 requires exactly that class, explicitly not `Schema.TaggedErrorClass`, so the errors cannot be Schema-derived where they are defined. The mapping therefore lives in one place with a **round-trip property over generated policies** standing in for the gate the policy codec gets. A record crossing a process boundary crosses a **trust** boundary — the reasoning ADR-QD-002 applies to policies — so `decodeRecord` validates rather than casts, and a payload naming a policy shape the ADT does not have is refused rather than walked. `ERROR_CODES`, which has said since it was written that it exists "for logging and cross-process correlation", is finally used for that: written on encode, and **ignored on decode**, because trusting a sender's code to choose a class would let it name one error and receive another. Two honest losses are recorded rather than hidden: an error's `cause` is `unknown` and is rendered to a string (an `Error` keeps its message; a value whose `toString` throws yields a marker, since the encoder a transport calls must never break what it observes), and an explicitly-`undefined` optional arrives absent because `Schema.optional` drops absent keys. A decision record naming neither outcome decodes to a `Failed` saying so — unreachable for anything this library encodes, but the wire is untrusted, and a row reading "the sender sent neither outcome" beats a dropped record |
| CCR-QD-062 | 2026-08-24 | **The last of the devtools gaps closed in code, and the one open security default decided.** **Obligations**: an allow carrying a binding duty nobody discharges is refused at the enforcement boundary, so a log of decisions alone showed such a request as `ALLOW` while the caller received an error. `ObligationRecord` now reports the gate as `Discharged` / `HandlerFailed` / `Refused` / `NotRequired` — per decision, not per obligation, because `ObligationHandler` takes the whole array and returns `void`, so which individual duty was met is not knowable without changing the contract, and a handler reporting falsely would be unverifiable anyway. The sink's payload became a tagged union to carry it, since discharge happens in `Qadi.ts` after `evaluate` has already emitted. **Ports now say what they are**: a service value was an anonymous object literal, so an operator seeing "everything denies" could not see that `AttributeResolverNone` was wired — optional `name`, set by every shipped implementation, composed by the wrappers, and nothing branches on it. **Port activity is counted** via `qadi_port_calls_total` and `qadi_port_retries_total`; metrics rather than the sink, because `MetricRegistry`'s default is memoised and so readable with zero wiring, where per-decision correlation would mean threading a collector through `evaluateNode` and risking INV-QD-005 for a debug view. **`asked()`** records the questions an atom set was asked, which is the honest basis for a gates panel: `Atom.family` keys structurally, so several `<Can>` on one policy are one atom, and DOM highlighting is dropped rather than bought with an instance registry AGENTS.md §13 forbids. **`/__permissions` is guarded by default** — it publishes every guarded path and the permission each requires, and shipped as a bare constant with no guard while the overview presented mounting it as ordinary wiring; `permissionRegistryRouteUnguarded(reason)` is the explicit opt-out and warns on every request. Two things were **refused** rather than built: a cache TTL, because its natural use is exactly the backend-revocation hazard `DecisionCache`'s own doc warns about, and per-obligation discharge state. Also: five package READMEs — every npm page would have rendered blank — plus `homepage`, `bugs`, `engines` and keywords on all five |
| CCR-QD-061 | 2026-08-24 | **Six gaps the devtools audit found, resolved in code rather than recorded in a document.** Marking a gap in markdown records a problem; it does not fix one, and every item here was data the library computed and discarded, or a question it could pose and not answer. **`policyDepth`** — `maxDepth` is an evaluation input, so nothing on a policy recorded how deep it was and every caller wanting to know had to re-walk the tree and guess at the convention; the agreement with the evaluator is the invariant (INV-QD-037), asserted against `evaluate` in both directions, because a depth under-reported by one declares safe exactly the input a caller meant to reject. **`permissionProvenance`** — `flattenPermissions` holds the granting role's name in its own closure and calls `keys.add` without it, so "via viewer" was unanswerable; kept a separate function because the flatten runs per subject construction, and held in agreement instead (INV-QD-038). **`diffTraces`/`flippedAt`** — "the node that flipped it" had no implementation at all; `isMismatch` compares verdicts and names nothing. **`getOrCompute` reports its outcome** and a record carries it, so "was this decision cached" stops being a process-global frequency shared by every cache in the process; absence and `"miss"` are kept distinct. **`DecisionCacheShape.clear`** — a cache could be emptied only by discarding its layer scope, which a tool inside that scope cannot do. **`resolveRoleGraph` reports unknown parents** — the lenient drop is right and stays, the silence was the defect: a typo in one parent name granted fewer permissions than its author wrote with nothing said, the same shape as `dehydrateDecisions` before `onDropped`. Separately, **`@qadi/react` now threads the seeded evaluation id into its re-check**, which is what makes the paired timeline the unified-stream draft describes actually joinable — the mechanism landed in CCR-QD-060 and nothing used it |
| CCR-QD-060 | 2026-08-23 | **The decision sink — the first channel by which a decision can be observed at all.** A devtools design proposed seven screens governed by a draft ADR saying the tool would consume Effect's tracing stream and add no evaluator surface. Audited against the code, that draft **could not deliver its own feature list**: `@qadi/core` had no observer channel of any kind (ADR-QD-009 deleted all four, and `DecisionHistory` is a *read* port whose header rejects writing), and a span cannot carry what a reader of a denial needs — attributes are flat primitives, a `Trace` is a tree. Exactly one of the seven screens was reachable through spans, and it was missing a column. **`DecisionSink`** is added on the terms ADR-QD-031 set for `DecisionCache`: optional, read through `Effect.serviceOption`, contributing nothing to `EvaluationServices`, so an application that wires none is unaffected. It is not what ADR-QD-009 removed — that was always-on bespoke machinery duplicating Effect's own channels; this is absent unless wired, one method, and carries what spans structurally cannot (ADR-QD-044). **A sink cannot change a decision** (INV-QD-035), enforced twice because once was already proven insufficient here: `record` returns `Effect<void>`, and — since BEH-QD-175 recorded `Effect.die` as exactly how a `never` channel gets subverted — the call site also wraps it in `Effect.catchCause`. Note this is the *opposite* call from BEH-QD-175, and deliberately: an extractor that cannot reach its store must change the answer, a sink must never be able to. **A record is complete** (INV-QD-036): it carries the `policy`, `resource`, `action` and start time a `Decision` never did — the policy most of all, because `explain` takes a `Policy` while a `Decision` carries a tag string, so **the explanation of a denial was unreachable from the denial**. **An `EvaluationError` reached no observer whatsoever** — no span attribute, no metric, no log — so a broken attribute store read as a *drop in traffic*; `DecisionOutcome` is now `Decided | Failed` and `qadi_evaluation_errors_total` is added. `EvaluateOptions.evaluationId` is added opt-in so a client re-check can name the server decision it continues; the default is untouched and ADR-QD-012 is amended rather than quietly contradicted. Also corrected: **four drifts in this document's own contents tables**, none of which anything gates — behaviour 19 listed as 145–151 and 21 as 161–165 when `traceability.md` had both right, behaviour 23 absent since CCR-QD-059 added it, and the ADR count given as "Thirty-nine" in one paragraph and "forty-three" in the next |
| CCR-QD-059 | 2026-08-23 | **The HTTP boundary made fail-closed, and the behaviour document `@qadi/http` never had.** The package shipped under ADR-QD-036 with **no `spec/behaviors/` entry, no BEH-QD identifiers and no traceability rows** — it entered the chain at the Decision link, so nothing normative sat between the ADR's prose and the code. That is how it came to contradict its own ADR in the place the ADR was most explicit. **(1) An unannotated endpoint was served.** ADR-QD-036's Alternatives section rejected "annotate-and-forget" by name — "it inverts this library's fail-closed posture … by making the *absence* of a permission requirement mean 'unguarded'" — and the rejected alternative shipped, with a test asserting it correct. Forgetting one annotation published an endpoint. Absence is now refusal (500, logged with the identifier, because a wiring mistake reported as 403 sends an operator to audit permissions), and `publicEndpoint(reason)` is the opt-out; the reason is required and never read, so being public is visibly a choice. INV-QD-034 — the only invariant here whose violation was written down as a rejected design *before* it was built. ADR-QD-036 Rev 1.3 records the divergence rather than quietly restating the rule. **(2) `SubjectExtractorShape.extract` had a `never` error channel**, so a broken token store could only `Effect.die` — escaping both adapters' `catchTag` and making an authorization path a defect, which AGENTS.md §4 forbids — or degrade to `anonymous`, rendering an outage as a denial. It now fails with `SubjectExtractionFailed` → 502, in both adapters; `guardRoute`'s declared `never` error channel is what forced the second arm to exist. A request carrying no credential remains a success resolving to `anonymous`. **(3) The Bearer scheme was matched case-sensitively**, so a legal `bearer …` (RFC 7235 §2.1 makes auth-scheme case-insensitive) had its credential discarded and was served as anonymous — a parsing bug presenting as a permissions problem. **(4) `PolicyTooDeep` mapped to 400**, blaming the caller for a policy no request can supply, and 400 is non-retryable client error to every SDK and dashboard — so the operator was never paged. Now 500, with the other wiring mistakes. Behaviour 23 covers all four plus the package's existing surface, and records two things it does **not** resolve: `/__permissions` is unauthenticated, and the registry reports the *permission* while `guard` evaluates the *policy*, so two endpoints reporting one permission may enforce unrelated rules |
| CCR-QD-058 | 2026-08-23 | **An audit of `@qadi/http` found two fail-opens, both in `@qadi/core`.** Neither was visible from the HTTP package and no gate caught either; both were reproduced before being fixed. **(1) `guard` evaluated the policy without the guarded resource.** `Qadi.ts` passed `resource` to the *handler* and ran `enforce(policy, options)` with `options.resource`, which no caller set — so `guardRoute`'s central parameter was authorization-inert. The direction is **fail-open**, not fail-closed: an absent resource does not deny, because a `ResourceRef` resolves to `undefined` and `neq` against `undefined` is `true`. A policy of `hasAttribute("homeTenant", neq(resource("tenant")))` served a subject whose home tenant *was* the resource's, and handed the handler an `Authorized<P>` witness for a check that never ran. It survived because the package's fixture uses a subject-only policy, against which an empty resource and a correct one are indistinguishable. INV-QD-032, BEH-QD-055 — the first requirement `guard` has ever carried, `guard`/`Authorized` having shipped under ADR-QD-035 with no behaviour identifier beneath it. Side effect: `@qadi/http`'s `NO_RESOURCE = {}` now denies a resource-scoped endpoint policy (403) where it previously failed with `MissingResource` (500), which is what its comment had always claimed. **(2) The decision cache keyed on `subject.id`.** `DecisionCache.ts` defended this as "not a leak across subjects, since the key includes the subject" — but an id is a sound proxy for a subject only if it determines that subject's grants, and `SubjectExtractor` rebuilds an `AuthSubject` per request from a token, so a scoped token and a full token for one user share an id. Under an application-scoped cache the first verdict won permanently, **in both directions**: a scoped token receiving a full token's allow, and a full token trapped by a scoped denial. The key now carries the whole `AuthSubject`, which compares structurally with its `HashSet` grants. INV-QD-033, BEH-QD-168 amending BEH-QD-163. Verified by falsification — erasing the grants from the key reproduces `[true, true]` where `[true, false]` is correct — and a control pins that equal subjects still hit, so the fix cannot pass by disabling the cache. Staleness is narrower as a result and BEH-QD-164 was narrowed to match: application scope is safe against token downgrade, unsafe against backend revocation. **Both were defended by a doc comment asserting the property that was missing**, which is why neither was noticed; with ADR-QD-042's stringified key that is three. ADR-QD-043. **Also: CI had been red on every clone since `b02c0f6`.** Three links from ADR-QD-036 and ADR-QD-037 pointed into gitignored `.scratch/`, so gate 8 passed on the author's machine and failed everywhere else — five commits of it. The links now name tracked artifacts, and the link check fails on a gitignored target rather than only a missing one, so the next occurrence fails locally instead of only in a CI nobody was reading |
| CCR-QD-057 | 2026-08-23 | **Production-readiness sweep: five claims the library made that were not true.** (1) `.changeset/config.json` said `access: "restricted"`, so the first `pnpm publish` of a scoped package would have failed with 402 — nothing had ever run, and gate 11 packs a tarball without touching registry access, so no gate could catch it. (2) `@qadi/react` carried no `"use client"` and had **no server-rendering test of any kind**. Directives are per-file, not on the barrel: a blanket one turns `dehydrateDecisions` into a client reference, and it exists to be *called* during server rendering. `"use client"` does not disable SSR — a Client Component is still rendered to HTML first — and `ServerRender.test.tsx` now proves it through `renderToString`, including the claim BEH-QD-145 makes and nothing had checked: a seeded decision visible in the first HTML. Writing that test corrected a wrong assertion of my own — a *synchronous* policy answers on the first read and never observes its seed (BEH-QD-151), so the control case had to be a policy that reaches a resolver. TypeScript 7.0.2 preserves the directive through `tsc -b`, verified against the emitted `lib/` rather than assumed. (3) `renderExplanation` never parenthesised, so `anyOf([a, allOf([b, c])])` and `allOf([anyOf([a, b]), c])` — which are not the same policy, the first admitting a lone `a` — rendered to a byte-identical sentence, and an obligation on one branch read as owed by the whole. INV-QD-031, BEH-QD-137. The flagship BDD scenario's expected string changed, by design. (4) `DecisionCache.keyOf` was `JSON.stringify`, and its own doc comment defended that as the option with "no chance of colliding" — **backwards**: stringify maps a `Date` onto its ISO string, drops `undefined`- and function-valued properties, and renders `NaN` as `null`, so two questions shared one entry and the second caller got the first's verdict. That made INV-QD-025 *false*. `HashMap` already keys structurally, so the fix is a **deletion** — `keyOf` is gone and the key is the struct; INV-QD-030 mirrors INV-QD-001 one layer down. Property order now hits, which had been documented as a deliberate miss. (5) `dehydrateDecisions` dropped every entry not matching the first subject's in silence; `onDropped` takes ADR-QD-041's shape, and its default message names a count but no subject and no policy, since a dropped decision belongs to another user. Also fixed: §1 declared behaviour 21 as BEH-QD-161–165 while the file had held BEH-QD-166 since it was added |
| CCR-QD-056 | 2026-08-23 | **A superseded seed is announced.** CCR-QD-052 made the client's answer win, silently — so a client wired differently from the server produced a guarded control that renders on first paint and vanishes on hydration, on every page, with nothing naming the cause: a configuration error presenting as a rendering glitch. BEH-QD-152, ADR-QD-041; behaviour 19 Rev 1.2, behaviour 09 Rev 2.3. `makeQadiAtoms` gains an optional `QadiAtomsOptions`; the default is a development-mode `console.warn`, replaced by `onHydrationMismatch`, which runs in production because server/client authorization disagreement is signal — it can indicate a page cached and served to the wrong user as readily as a wiring error. Three scoping rules, each stated rather than left implicit: a mismatch is a difference of **verdict**, since field-set differences would bury the case that matters; a client-side **failure** is not a disagreement, which is INV-QD-006 in reverse and the same rule BEH-QD-072 applies to a function `fallback`; and it reports **once per question**, since the seed outlives every invalidation. **First `console` and first `process.env` in the repository**, confined to `HydrationWarning.ts` for the reason `EvaluationId.ts` confines `crypto.randomUUID` — the literal `process.env.NODE_ENV` text is load-bearing rather than idiomatic, because that exact member expression is what a bundler folds; the confinement is a convention and not a gate, and that is recorded rather than claimed otherwise. The message names the policy from `decided.trace`, never `seeded.trace`: a hydrated trace is a reduced projection and without `includeTrace` names nothing — **the first draft read the seed's tag and printed the wrong policy name**, caught by the test rather than by review. Gate 9 rejected `export type { … } from` outright; its own message asked to be taught, so it now understands the form rather than being worked around |
| CCR-QD-054 | 2026-08-23 | **A guard hands its denial to the node that replaces it.** `Can`'s `fallback` and `Cannot`'s `children` accept a function of the `Deny`; BEH-QD-072, and `DeniedNode` added to `@qadi/react`. The same defect as CCR-QD-053 at a different surface — `components.tsx` held the denial, with its reason and its whole trace, at the exact moment it decided to render nothing, and discarded it, so "why is this control not here?" was the one question the declarative API could not answer while the answer sat one argument away. A plain node stays the common case, so the type is a union rather than a required function. One rule falls out and is stated rather than left implicit: **a function `fallback` is not reused for the failure branch**. `failure` still defaults to a *node* fallback, but a function fallback is written to explain a refusal and during an outage no refusal happened, so calling it would describe one that never occurred — INV-QD-006 at the component layer. It renders nothing instead, which is still closed, and the test proves the failure branch is genuinely reached by showing a node fallback rendering in the same wiring |
| CCR-QD-053 | 2026-08-23 | **A denial explains itself where it surfaces.** `renderTrace` added to `@qadi/core` — the decision-side counterpart to `renderExplanation`, and the second rendering the library assembles; BEH-QD-144. `AccessDenied` gained the `trace` field its own doc comment had claimed since it was written ("Carries the decision so callers can inspect the trace" — it did not); BEH-QD-054. Until now the whole tree was built and dropped at `Qadi.ts`, so the one path most callers meet a denial on — `assert`, `enforce`, `enforceProjected`, `guard`, the Promise rejection, the HTTP mapping — kept a single sentence, and "which branch refused?" could only be answered by dismantling the enforcement wiring and re-running `decide`. Deliberately **not** crossed: a trace names every node's tag, its label and why it refused, so `toResponse` still returns an empty body and hydration still withholds the trace by default. `renderTrace` takes no `Policy` and so never claims which branches went unevaluated — INV-QD-020 discards them, and a renderer that guessed a count would be inventing one |
| CCR-QD-052 | 2026-08-23 | **A client-side authorization bypass closed.** A server-rendered decision was seeded straight into the decision atom, where `AtomRegistry` marks the node `preserveInitialValueOnBuild` and, on the first build, keeps the seed and discards what the read returned. An **asynchronous** evaluation escapes that by publishing through `setSelf` on a later turn; a **synchronous** one publishes by returning, and was the value discarded. Every policy needing no resolver evaluates synchronously and `AttributeResolverNone` is the shipped default, so the common case was: the server sends an allow, the client decides **deny**, and the denial is thrown away for the life of the page. BEH-QD-151, INV-QD-028, ADR-QD-039 (amending ADR-QD-028); behaviour 19 Rev 1.1. The seed now lives in its own atom and is consulted only while the computed result is `Initial` — the one state meaning this client has never answered — so a re-check in flight and a client-side **failure** both keep it spent. `Hydration.test.ts` could not see the defect because every test read the registry on the tick it was built, which asserts a seed is *present* and can never observe whether it is *superseded*. Note what ADR-QD-017 did not catch: the bypassed value was not `waiting`, so `currentDecision` returned it and every consumer was correct — **that rule guards the flag, and this failure never set it**. Recorded narrowing: BEH-QD-148's `evaluationId` correlation still holds of the payload and the seeded decision, but a synchronously-evaluated policy answers on the first read, so a consumer reads its own id. Also here: **CCR-QD-041–051 backfilled below**, the register having recorded nothing since CCR-QD-040 while eleven were in use in `traceability.md`, `roadmap.md`, `glossary.md`, `definitions-of-done.md` and four ADRs; and the ADR count corrected from thirty-four to thirty-nine |
| CCR-QD-051 | 2026-08-22 | Roadmap Rev 1.25 — the two new merge gates (`madge`, `tstyche`) and changesets recorded; gate table updated (ADR-QD-037, ADR-QD-038) |
| CCR-QD-050 | 2026-08-22 | Traceability Rev 1.26 — ADR-QD-037 and ADR-QD-038 traced; both record tooling gates rather than authorization invariants, so both carry `—` in the invariant column |
| CCR-QD-049 | 2026-08-22 | **Changesets adopted** — ADR-QD-038. A version bump per publishable package, with `access: restricted` and no ignore list. Publishing itself still runs nowhere: the repository has no release workflow, and `changeset status` is deliberately **not** a merge gate, so `pnpm check` remains the whole definition of done |
| CCR-QD-048 | 2026-08-22 | **Two new merge gates** — `madge --circular` as step 5 and `tstyche` as step 6; ADR-QD-037. Both sit after the lint family and before the runtime suite, on the same reasoning that already put the doc-example gate ahead of mutation testing: each takes under two seconds, so a regression fails before anything slower starts. Records a standing gap rather than hiding it — `tstyche` pins TypeScript 6.0.3, the newest it supports, against a workspace on 7.x, so a `.tst.ts` assertion passing is not the same claim `tsc -b` makes |
| CCR-QD-047 | 2026-08-22 | Roadmap Rev 1.24 — the witness/`guard` primitive and `@qadi/http` moved to Shipped; gate figures refreshed (ADR-QD-035, ADR-QD-036) |
| CCR-QD-046 | 2026-08-22 | ADR-QD-036 Rev 1.2 — **three corrections found by the first real HTTP round-trip test**, each one a claim that had survived review. `requiresPermission` is not `.pipe()`-composable **at all**: the widened return type it shipped with in Rev 1.1 breaks `HttpApiBuilder.group`'s handler exhaustiveness for the whole group, not merely later chaining. Its parameter type is a minimal structural `AnnotatedEndpoint`, not `HttpApiEndpoint.Top` — a param-less endpoint is not actually assignable to `Top`. And `GuardRoute.ts`'s `guardRoute` under-excluded `CurrentSubject` from its declared return type |
| CCR-QD-045 | 2026-08-22 | ADR-QD-036 Rev 1.1 — `requiresPermission`'s type-preservation claim corrected: it does not survive a reusable generic wrapper, and the shipped signature returns the widened `HttpApiEndpoint.Top` instead |
| CCR-QD-044 | 2026-08-22 | Traceability Rev 1.25 — ADR-QD-035 and ADR-QD-036 traced |
| CCR-QD-043 | 2026-08-22 | Glossary Rev 1.5 — **Witness** and **Guard** added; the Revision field corrected to match its latest entry, having drifted since CCR-QD-019 |
| CCR-QD-042 | 2026-08-22 | **`@qadi/http` built**, a sixth workspace package; ADR-QD-036 Accepted. One enforcement path bound to both of Effect v4's HTTP surfaces — `RequirePermission` for `effect/unstable/httpapi` and `GuardRoute` for `HttpRouter` — plus `SubjectExtractor`, `QadiHttpError` and a `PermissionRegistry` whose `/__permissions` route answers which permission each endpoint requires. A denial is a **403 before the handler runs**, which is what makes the binding an enforcement boundary rather than a convenience |
| CCR-QD-041 | 2026-08-22 | **The witness/`guard` primitive** shipped in `@qadi/core`; ADR-QD-035 Accepted. `guard` produces `Authorized<P>` — a branded value proving a specific `Policy` check succeeded against a specific resource — so a handler typed to require one cannot be called without going through enforcement first. It extends the guarantee `enforce` gives an `Effect` to a value a type signature can demand, and it is framework-independent by construction: `Context` cannot prove *which* permission a witness is for, which is why the witness travels as a value |
| CCR-QD-040 | 2026-07-27 | The benchmark AGENTS.md §5a had deferred to for its whole life, and the finding it turned up. `packages/core/bench/` with `pnpm bench`; ADR-QD-034. **The measurement**: at the dispatch site a `switch` is **1.6–2.4×** faster than a hoisted `Match` returning a closure and **3.5–7.7×** faster than `Match.value` rebuilt per call, which is what a naive conversion produces; end to end that is **2–4%** on a matcher-heavy policy and **under 1%** on a simple one. Reported as ranges because absolute throughput on the development machine moves ~30% between runs — a figure quoted to three significant figures from one laptop run is the evidence ADR-QD-025 replaced. So the exception is real but small, and stands. **The finding that mattered more**: `Match.tagsExhaustive` makes a new tag a compile error and a `switch` does so only if its return type cannot absorb `undefined`. Tested by adding a sixth `ValueRef` member — `evaluateNode` (concrete `Effect`) and `evaluateMatcher` (`boolean`) are caught by TS2366; **`resolveRef` (`unknown`) and `mergeFields` (`… | undefined`) were not**, and those are exactly the two CCR-QD-039 found undeclared. A rule kept by memory had lost the two cases needing it most. They fail in opposite directions: `resolveRef` resolves to `undefined` and denies everything, while `mergeFields` merges to the **top** of the field lattice and *widens* visibility (INV-QD-004) — fail-open on the dimension this library exists to protect. Both now carry a `default` arm assigning the scrutinee to `never`: free at runtime, and it fires (TS2322) alongside the two `Match` dispatchers in `Explanation.ts` and `Predicate.ts` that always caught it. Cost accepted **without** suppression pragmas, of which this repository has no precedent: lines 100% → 99.55%, branches 98.41% → 98.02%, mutation 90.25% → 90.03%, all still clear of their thresholds, because an unreachable branch genuinely is not covered and a report saying so is correct. `packages/*/bench` added to `tsconfig.test.json` so a benchmark cannot rot away from the API it measures; `pnpm bench` is deliberately **not** a gate, since a timing threshold on a shared runner fails for reasons unrelated to the change |
| CCR-QD-039 | 2026-07-27 | AGENTS.md §5a made enforceable, after it was found to be enforced by memory alone. The section banned dispatching on a `_tag` with `switch` and named **two** deliberate exceptions; there were **four**. `resolveRef` (`Matcher.ts`) switches on `ref._tag` and `mergeFields` (`Evaluate.ts`) on the `FieldStrategy` literal union — both undeclared, both with the *same* hot-path justification as the two that were written down, so the reasoning covered them while the enumeration did not. Gate 4 checked seven rules and `switch` was not among them. `scripts/check-house-style.mjs` now carries a `SWITCH_BUDGET` of file → exact count. A **count** rather than a per-file exemption deliberately: a blanket pass would let the next switch into the two hottest files in the library unseen, which is precisely where one would be added. It fails in **both** directions, so converting one to `Match` also fails until §5a and the budget are updated together. Three deliberate breaks all fire — a switch in an undeclared file, a third switch in a file budgeted for two, and a budget claiming more than exist. Also recorded: §5a defers converting these to a benchmark and **no benchmark exists**, so the cost is unmeasured and that is now stated as the reason the exception stands. No ADR and no invariant: nothing about runtime behaviour changed, `Match` and `switch` being semantically identical here, and house-style rules appear in `invariants.md` as enforcement mechanisms rather than as invariants |
| CCR-QD-038 | 2026-07-26 | `scripts/check-package-install.mjs` added as **merge gate 10**, the first gate that looks at the package rather than the sources: it reads the build graph, packs each public package with `pnpm`, extracts the tarballs into an offline sandbox and compiles and runs a TypeScript consumer against the shipped `.d.ts`. INV-QD-027, ADR-QD-033. Built because every test in the repository imports `src/` by relative path, so nothing had ever resolved a `@qadi/*` specifier through a published `exports` map. Checking by hand found **two real defects**. `npm pack` copies pnpm's `catalog:` and `workspace:` protocols into the tarball verbatim and the result cannot be installed at all (`EUNSUPPORTEDPROTOCOL`), so these packages are publishable with `pnpm` and **not** with `npm` — a constraint nothing recorded. And `tsconfig.build.json` had omitted `@qadi/promise` since the commit that added the facade (`dae3aa1`; the file had not been touched since the initial scaffold), so `pnpm build` emitted nothing for it and `pnpm publish` would have shipped that package **empty**. The second defect survived ten green gates because `pnpm typecheck` uses a *different* project graph that does include the package, and `tsc -b` emits — so a `lib/` was always on disk looking like a build product. That is why the gate's first check reads the build graph statically: it is the only one a stale directory cannot fool, and it was verified with the stale output deliberately left in place. Five deliberate breaks all fire — packing with `npm` (7 failures), an `exports` path with no file behind it, a renamed declaration (TS2305), an allow-turned-deny in the built evaluator (*effect allow was Deny, wanted Allow*), and the promise package removed from the build graph. Also corrected: `spec/roadmap.md` and ADR-QD-025 both still called `stryker` "step 9" while the gate table had it at 10 — pre-existing drift found while renumbering; ADR-QD-025 Rev 1.1 now names the step by position rather than index. AGENTS.md gained §16 (publish with `pnpm`, never `npm`; a new public package goes in `tsconfig.build.json` too), and the old §16 became §17 |
| CCR-QD-037 | 2026-07-26 | The 157 mutation survivors read, closing an open question `spec/roadmap.md` had carried since CCR-QD-026. **62 are `StringLiteral`** — `reason` wording, where surviving is correct. 52 affect logic and **one was a real gap**: `fieldMatch` used `isObject(value) && …`, and every existing test passed a non-object whose property was `undefined`, so `&&` and `||` agreed by accident; `"hello"` has a real `length`, so `fieldMatch("length", gte(3))` on it would have read a property off a primitive. Tested, and the test is shown to fail on the mutant and pass on real code. Two other alarming-looking survivors were **my own reproduction errors** — the JSON report names the replacement text, not the node it replaced. A hypothesis that `coverageAnalysis: "perTest"` under-attributed tests was tested against `"all"` and **disproved**: 157 either way. Stryker's `json` reporter enabled so this analysis is repeatable. Score 90.19% → 90.25% |
| CCR-QD-036 | 2026-07-26 | **CI added** — `.github/workflows/check.yml`, running `pnpm check` and nothing else. A workflow enumerating its own steps would be a second definition of "done" and would drift from `definitions-of-done.md` exactly as `spec/overview.md` drifted from the exports; this way adding a gate means editing `check` and the DoD table together, and CI follows. Pinned to Node 26 rather than floating on `latest`, so a toolchain change cannot report a failure that is not about the change under test. **Verified end to end**: `actionlint` clean, and the whole workflow executed locally under `act` — checkout, `pnpm/action-setup`, `actions/setup-node` (Node 26.5.0), `pnpm install --frozen-lockfile` and the full `pnpm check` all green in a clean Linux container, which also proves the lockfile is not stale. The artefact upload cannot succeed under `act`, which has no `ACTIONS_RUNTIME_TOKEN`, and is now `continue-on-error` on its own merits: a diagnostic upload failing should not turn a passing gate red. **The repository has no git remote, so this has never run on GitHub.** Also: `AGENTS.md` §15 and `README.md` both said "there is no CI" one commit earlier and are corrected here, and `engines` tightened from `>=20.0.0` to `>=20.19.0`, which is what `oxlint` actually requires. *A first attempt reported `actions/setup-node` failing with a self-signed certificate and that was recorded as network TLS interception — it was wrong. The host verifies `nodejs.org` cleanly and a re-run succeeded; it was a transient failure while downloading Node, and the claim was made from one observation.* |
| CCR-QD-035 | 2026-07-26 | Documentation drift swept repo-wide, beyond what gate 9 covers. **Six documents claimed runnable examples were "type-checked in CI" and there is no CI** — AGENTS.md, urs.md, spec/README.md and three model documents; all now name the merge gate that actually runs them. The root `README.md` announced **"Phase 0 — scaffold … nothing here is stable yet"** on a library with 459 tests and ten gates, and its `pnpm check` comment read "as CI runs it"; both corrected, and `@qadi/promise` added to its package table. `spec/README.md` was missing six behaviour documents and eight ADRs and still said "twenty-four ADRs"; two of its behaviour ranges were stale. `spec/traceability.md` §1 had **four** ranges its files had outgrown (01, 03, 05, 07). BEH-QD-020 was described as a "thirteen-variant union" when there are fourteen. Glossary entries added for explanation, simplification, join/meet, hydration and concurrency. AGENTS.md gained §14 (`@qadi/promise`: no branch may decide) and §15 (documentation is gated, not remembered — and do not write "in CI") |
| CCR-QD-034 | 2026-07-26 | `scripts/check-api-surface.mjs` added as **merge gate 9**: every export of every public package must be named in `spec/overview.md`, every name in its API tables must be a real export, and every workspace package must appear in its Packages table. Built because that document drifted **twice** — CCR-QD-025 fixed it, and six commits later it was missing ten exports and the whole `@qadi/promise` package, while BEH-QD-042 had gone from "four services" to "five" when there were six. Two occurrences in one session is a property of the process, not an oversight. The gate caught **94** drifts on first run and all four deliberate breaks. Two findings: `RelationshipResolver.ts` contains literal NUL bytes, so `grep` treats it as binary and a shell-based checker would lose five exports while reporting success; and matching **backticked** tokens rather than bare words is load-bearing, since `join`, `meet`, `check` and `filter` are ordinary English and `Matcher`/`Role` appear as filenames. `spec/overview.md` Rev 1.2 now documents 222 exports across four packages plus a declared-omissions table |
| CCR-QD-033 | 2026-07-26 | `@qadi/promise` built, a fifth workspace package: `makeQadi`, `check`, `decide`, `assert`, `filter`, `dispose`; BEH-QD-169–173, INV-QD-026, URS-QD-028; ADR-QD-032 Accepted. One file, every method `runPromise` over a core function, **no branch that decides anything** — which is what makes "never a second evaluator" checkable by reading. A **denial resolves and a failure rejects**: `try { check() } catch { return false }` is the natural Promise idiom and would turn an attribute-store outage into a silent lockout, so INV-QD-006 is asserted again at the edge. The subject travels per call, since a runtime holding one subject would be a per-process subject. Agreement with the core asserted over both paths including the whole trace. `Under consideration` is now empty |
| CCR-QD-032 | 2026-07-26 | Decision caching built: `DecisionCache`, `decisionCacheLayer`; BEH-QD-161–165, INV-QD-025, URS-QD-027; ADR-QD-031 Accepted. Optional by construction — read with `Effect.serviceOption`, so it is not in `EvaluationServices` and no existing caller changes. It caches the **`Trace`**, never the `Decision`: caching the decision whole would duplicate `evaluationId` across two evaluations, so two log lines would claim to be one event and correlation would stop working; the id and duration are stamped per call, hit or miss, and the identity clause is asserted as an **inequality**. The key is `subjectId + policy + resource + action` — a second instance of the rule that any structure holding decisions holds the subject. `concurrency` is deliberately excluded from the key. Found while testing: `Effect.provide` builds a layer per execution, so a cache piped onto one `evaluate` caches nothing while reading correctly — asserted rather than noted |
| CCR-QD-031 | 2026-07-26 | Policy simplification built: `simplify`; BEH-QD-153–156, INV-QD-024, URS-QD-026; ADR-QD-030 Accepted. Left *Under consideration* by being built **because of** its objection: rewriting the tree changes the trace, so it is opt-in and nothing in the library calls it. Two conditions, both disclosure hazards rather than verdict ones — flattening is sound only when the field strategies match, and **double-negation elimination is unsound**, because `Not` carries `visibleFields: undefined` and no obligations by design, so `not(not(p))` allows with every field and owes nothing. That rewrite was written and the property over policies × four subjects rejected it: the second time a property has paid for itself by contradicting something obvious |
| CCR-QD-030 | 2026-07-26 | `join` and `meet` shipped on `SecurityLabel`; BEH-QD-103–104, INV-QD-023, URS-QD-025, three scenarios under `@REQ-QD-021`; ADR-QD-029 Accepted, **reversing ADR-QD-021's decline** on MOD-QD-029's own unanswered argument. BEH-QD-100 withdrawn. The distinction ADR-QD-021 drew was right — deriving a label is not deciding an access, now BEH-QD-104, and `Evaluate.ts` imports neither function — but "do not compute a label during evaluation" and "do not export the function a caller needs" are two decisions and only the first had an argument. The failure it prevents is silent: take the higher level, carry *its* compartments, and the derived object is labelled below its own contents while every comparison stays correct. MLS moves to **Shipped**, closing the contradiction CCR-QD-024 recorded — the model defines a lattice as "a partial order with joins" and E4 had shipped only the order |
| CCR-QD-029 | 2026-07-26 | Server-side rendering built: `dehydrateDecisions`, `hydrateDecisions`, `DehydratedDecisions`; BEH-QD-145–150, INV-QD-022, URS-QD-024; ADR-QD-028 Accepted. The roadmap called it a hydration story; it is a **security** story — a payload is the only place a decision enters the library without being evaluated by it, so nothing else would catch a page cached across users. Bound to a subject id, refused as a whole on mismatch, trace withheld by default, and both refusals **drop** rather than throw so the failure degrades to the pre-hydration flash. Works only because `Atom.family` keys structurally (BEH-QD-071): a re-parsed policy is equal and maps to the same atom, which is why policies identify themselves rather than needing caller-supplied keys nothing could verify |
| CCR-QD-028 | 2026-07-26 | Policy explanation built: `explain`, `renderExplanation`, the `Explanation` tree; BEH-QD-137–143, INV-QD-021, URS-QD-023, `@REQ-QD-023`; ADR-QD-027 Accepted. The roadmap asked for a string and got a **tree**, on ADR-QD-024's "Qadi owns no dialect" argument, with English confined to one function. Two things the roadmap entry did not anticipate: an explanation must state **restrictions** as well as requirements, because describing a field-narrowed permission as a bare requirement overstates the grant; and it takes **no subject**, which is the whole distinction from a trace and prevents an admin screen leaking who satisfies what. The third interpreter over the policy tree, and the only one with no agreement property — an explanation is prose about a policy, so totality and node-count correspondence are asserted instead |
| CCR-QD-027 | 2026-07-26 | Concurrent evaluation built: `EvaluateOptions.concurrency`; BEH-QD-129–135, INV-QD-020, URS-QD-022, `@REQ-QD-022`; ADR-QD-026 Accepted. INV-QD-005 **scoped** to sequential evaluation rather than repealed. The decision and the full trace are identical with and without it, because the combining rules live in one fold per composite that both paths drive in declaration order — so the concurrent path discards trace nodes for children evaluated after the decisive one. Roadmap item, unblocked by E3 and constrained by it: the deciding rule of a `Rules` node is still selected by index, never by arrival, or two runs of one table would owe different duties |
| CCR-QD-026 | 2026-07-26 | Mutation testing shipped as step 9 of `pnpm check`, breaking below 80% — ADR-QD-025. Score **89.22%** over 1345 mutants; 141 survived, 5 timed out. Replaces five hand-run passes quoted into ADRs as prose, which nobody but their author could reproduce. Three non-obvious workarounds documented as load-bearing: `tsconfigFile` names a file that does not exist because Stryker rewrites tsconfigs through an API TypeScript 7 removed; `plugins` is explicit because the default glob does not follow pnpm's double symlink; `vitest.related` is off because it resolves paths against a different root. The DoD gate table also gained the `spec:examples` row it had been missing while `pnpm check` ran it. First enforced run surfaced a finding recorded as a new Planned item: `Evaluate.ts` scores 77.85%, the lowest of any file that matters |
| CCR-QD-025 | 2026-07-26 | The `DecisionHistoryUnknown` fence sweep, and the two documents it exposed. `spec/overview.md` was still at Rev 1.0 and its "Public API surface" described the library as it stood before **any** of the seven enablers shipped — twenty-one exports and four errors missing. `spec/behaviors/06-services.md` was headed "The four services" with a four-row table while `05-evaluator.md` had listed five since E5, and `BEH-QD-043` omitted `DecisionHistoryUnknown`, the one default that needed three values to be closed in both polarities; the same error had propagated into §1 of the traceability matrix. Seventeen compiled examples then gained the layer. Two further misses found by checking `Layer.mergeAll` arguments rather than files — `25-rubac.md` and `30-chinese-wall.md` each mention the layer once and were counted as done by CCR-QD-019 and CCR-QD-022. No revision bumped on the seventeen: an example correction is not a change of claim |
