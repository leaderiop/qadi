# Requirements

No PRD documents were present in this classification batch (`CLASSIFICATIONS_DIR` contained 50 files, all `type: ADR`). No entries to synthesize.

If PRDs are ingested in a future batch, requirements extracted from them will be added here as `## REQ-{slug}` entries with `source`, `description`, `acceptance`, and `scope` fields, per the doc-synthesizer output contract.
