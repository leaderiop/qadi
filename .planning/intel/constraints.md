# Constraints

No SPEC documents were present in this classification batch (`CLASSIFICATIONS_DIR` contained 50 files, all `type: ADR`). No entries to synthesize.

Note: `spec/behaviors/*.md`, `spec/invariants.md`, and `spec/models/*.md` exist in the repository and are referenced heavily by the ADRs' `cross_refs` (e.g. `INV-QD-005`, `INV-QD-006`, `BEH-QD-072`), but none were included in this classification batch and so were not read or extracted here. If they are ingested in a future batch, constraints extracted from them will be added here as `## {title}` entries with `source`, `type` (api-contract | schema | nfr | protocol), and `content` fields.
