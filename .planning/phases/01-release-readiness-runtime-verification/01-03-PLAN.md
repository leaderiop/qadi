---
phase: 01-release-readiness-runtime-verification
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - .changeset/config.json
  - .changeset/audit-signature-harmonization.md
  - .changeset/audit-invariants-mutation-gate.md
  - packages/audit/package.json
  - packages/core/package.json
  - packages/devtools/package.json
  - packages/http/package.json
  - packages/predicate-prisma/package.json
  - packages/predicate-sql/package.json
  - packages/promise/package.json
  - packages/react/package.json
  - packages/testing/package.json
  - packages/audit/CHANGELOG.md
  - packages/core/CHANGELOG.md
  - packages/devtools/CHANGELOG.md
  - packages/http/CHANGELOG.md
  - packages/predicate-prisma/CHANGELOG.md
  - packages/predicate-sql/CHANGELOG.md
  - packages/promise/CHANGELOG.md
  - packages/react/CHANGELOG.md
  - packages/testing/CHANGELOG.md
  - package.json
autonomous: false
requirements: [REL-03]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.changeset/config.json` declares `fixed` as exactly one nested group — an array containing one array — naming exactly the nine public packages, and `access` still reads `public` (D-10)."
    - "All nine public `packages/*/package.json` files read exactly `0.3.0`, and the private workspace root `package.json` reads `0.3.0` too (D-11, D-12)."
    - "The version number was computed by `pnpm changeset version` from the recorded changesets, not written by hand into nine manifests (D-09)."
    - "`.changeset/` holds no unconsumed changeset after the run — only `README.md` and `config.json` remain, every `*.md` changeset having been folded into a package CHANGELOG."
    - "Two changesets naming `@qadi/audit` at `minor` produced one bump, not two: the package lands at 0.3.0 with both bodies present in its CHANGELOG."
    - "A public package with no direct changeset of its own is still carried to 0.3.0 by the fixed group — the group has no empty-input hole, which is the whole point of choosing lockstep versioning."
    - "Each of the nine packages has a `CHANGELOG.md`, where none existed before this plan."
  artifacts:
    - .changeset/config.json
    - packages/{audit,core,devtools,http,predicate-prisma,predicate-sql,promise,react,testing}/package.json
    - packages/{audit,core,devtools,http,predicate-prisma,predicate-sql,promise,react,testing}/CHANGELOG.md
    - package.json
  key_links:
    - "`.changeset/config.json` `fixed[0]` -> the nine `packages/*/package.json` `name` fields. A typo in one name silently drops that package from the lockstep group; `changeset version` will not warn, and the package ships at a different version from its siblings — the exact failure the fixed group exists to prevent."
    - "`fixed` group membership -> the private root `package.json`. The root is deliberately outside the group and is `private: true`, so `changeset version` will not touch it. D-12's bump is a manual edit and will be silently skipped if assumed to have happened."
    - "The active Node runtime -> `@changesets/cli@3.0.1`'s own `engines` (`^22.11 || ^24 || >=26`). Running the version step under the Node 20.19.0 floor this phase adds to CI does not work at all."
  prohibitions:
    - "Nothing in this plan publishes. `pnpm changeset publish`, `pnpm publish` and `npm publish` must not run. Publishing is Phase 2, it is irreversible against the public registry, and a version bump landing in the working tree is not consent to ship it. AGENTS.md §16 also forbids the `npm` form outright regardless of phase."
    - "The nine version numbers must not be hand-written into the manifests, and the tool's computed output must not be overridden if it disagrees with the expected 0.3.0. If the tool computes something else, that is information about the recorded changesets and the checkpoint is where it gets resolved — ADR-QD-038 exists precisely so no person tracks cross-package version math by hand."
---

<objective>
Give all nine public packages one shared, real version number, computed rather
than asserted. Today they sit split across `0.1.0` and `0.2.0` with the private
workspace root at the placeholder `0.0.0`, and nothing has ever consumed a
changeset.

Purpose: REL-03 requires no package remain at a placeholder version, and D-10
makes the nine permanently lockstep so `@qadi/*` packages are never again
mutually ambiguous about which versions go together. D-09 pulls this into
Phase 1 deliberately, so Phase 2 is a pure publish with no version decision
left in it.

Output: `.changeset/config.json` carrying the fixed group, nine manifests and
the root at `0.3.0`, nine first-ever `CHANGELOG.md` files, and an empty
changeset queue.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-release-readiness-runtime-verification/01-CONTEXT.md
@.planning/phases/01-release-readiness-runtime-verification/01-RESEARCH.md
@.planning/phases/01-release-readiness-runtime-verification/01-PATTERNS.md
@.planning/phases/01-release-readiness-runtime-verification/01-02-SUMMARY.md
@.changeset/config.json
@AGENTS.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Created or Modified |
|---|---|---|
| `.changeset/config.json` -> `fixed` | JSON key value | modified (`[]` -> one nested group of 9) |
| `packages/{9}/package.json` -> `version` | JSON key value | modified (`0.1.0` / `0.2.0` -> `0.3.0`) |
| `packages/{9}/CHANGELOG.md` | markdown file | **created** — no package in this repo has ever had one |
| `package.json` (root) -> `version` | JSON key value | modified (`0.0.0` -> `0.3.0`) |
| every `.changeset/*.md` except `README.md` | changeset files | **deleted** by `changeset version`, bodies folded into the CHANGELOGs |

The nine packages are `audit`, `core`, `devtools`, `http`, `predicate-prisma`,
`predicate-sql`, `promise`, `react`, `testing` — all nine directories under
`packages/`, none of which carries `private: true`.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Confirm the computed release version and the exact lockstep group membership before the tool writes them</decision>
  <context>
D-10 (permanent fixed group across all nine `@qadi/*` packages) and D-11
(0.3.0, staying pre-1.0) are locked user decisions. This checkpoint does not
reopen them.

What it gates is the write. D-10 is recorded in CONTEXT.md as **one-way**: once
the nine ship under a shared version, consumers reasonably come to rely on
"install any `@qadi/*` package and they are always at the same version", and
unwinding the group later breaks that implied parity contract rather than just
a config file. The version bump itself is `costly` rather than one-way while it
sits in the working tree — `git revert` undoes it — but it becomes one-way the
moment Phase 2 tags and publishes, and nothing between here and there asks
again.

So the last cheap moment to notice a wrong number is now, before
`pnpm changeset version` rewrites nine manifests and deletes twenty-six
changeset files.

The arithmetic, for confirmation rather than for decision: the highest current
version among the nine is `0.2.0` (held by core, testing, react, promise). The
highest pending changeset severity is `minor`. A `minor` on `0.2.0` is `0.3.0`,
which is what D-11 records. `1.0.0` is deliberately not taken — the codebase is
feature-complete but the release stays pre-1.0.

The group is exactly these nine, and no more: `@qadi/core`, `@qadi/testing`,
`@qadi/promise`, `@qadi/react`, `@qadi/http`, `@qadi/devtools`, `@qadi/audit`,
`@qadi/predicate-sql`, `@qadi/predicate-prisma`. The private workspace root and
`@qadi/features` are not in it and must not be added — they are never published.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed — write the fixed group and run the version bump to 0.3.0</name>
      <pros>Matches D-10, D-11 and D-12 exactly. Phase 2 becomes a pure `pnpm publish` with nothing left to decide. Lockstep removes the ongoing question of which `@qadi/*` versions are compatible with each other.</pros>
      <cons>The lockstep scheme is one-way once published: every future release bumps all nine together whether or not a given package changed, and consumers will read parity into it.</cons>
    </option>
    <option id="halt">
      <name>Halt — the computed number or the group membership is not what was expected</name>
      <pros>Nothing irreversible has happened yet; twenty-six changesets are still on disk and can be inspected or amended.</pros>
      <cons>Blocks the rest of the phase until resolved. Phase 2 cannot proceed without a versioned release.</cons>
    </option>
  </options>
  <resume-signal>Reply "proceed" to write the fixed group and run the bump, or "halt" and say which of the version or the group membership is wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Configure the permanent nine-package fixed group</name>
  <files>.changeset/config.json</files>
  <read_first>
    - `.changeset/config.json` — all twelve lines. The `fixed` field is currently `[]` and is the only field this task changes.
    - `.planning/phases/01-release-readiness-runtime-verification/01-RESEARCH.md` — Pattern 2, which cites the official changesets docs for the `fixed` field's shape: an array of *groups*, where each group is itself an array of package names. The flat-array reading of D-10's prose ("a group containing all 9") is the mistake this pattern exists to prevent.
    - `.planning/phases/01-release-readiness-runtime-verification/01-PATTERNS.md` — the `.changeset/config.json` section, which reproduces both the current file and the target `fixed` value.
  </read_first>
  <action>
Set `.changeset/config.json`'s `fixed` field to one nested group naming exactly
the nine public packages, per D-10.

The shape is an array of arrays. `fixed` takes a list of *groups*; each group
is a list of package names. So the value is a one-element outer array whose
single element is a nine-element array of scope-qualified names. Writing it as
a flat array of nine strings is the shape mistake RESEARCH.md Pattern 2 calls
out — the `$schema` this file already references would likely reject it, but it
reads as plausible if D-10's prose is taken literally.

The nine names, exactly as they appear in each `packages/*/package.json`
`name` field: `@qadi/core`, `@qadi/testing`, `@qadi/promise`, `@qadi/react`,
`@qadi/http`, `@qadi/devtools`, `@qadi/audit`, `@qadi/predicate-sql`,
`@qadi/predicate-prisma`.

This is permanent, not a one-off for this release. Every future
`changeset version` run bumps all nine to one shared number from here on.

Change nothing else in the file. In particular leave `access` reading `public`.
RESEARCH.md flagged that it was changed at some point before this phase, by
someone, without a record — ADR-QD-038's text still describes the tool's
`restricted` default. It is not this phase's decision to make or unmake, and
`public` is what a real scoped publish in Phase 2 needs anyway. Leave
`changelog`, `commit`, `linked`, `baseBranch`, `updateInternalDependencies` and
`ignore` exactly as they are.

Do not add the root package or `@qadi/features` to the group. Both are
`private: true`; changesets skips private packages in every changeset-aware
operation, so listing them would be inert at best and misleading at worst.
  </action>
  <reversibility rating="one-way">D-10 records this as one-way: once the nine publish under a shared version, unwinding the group breaks an implied version-parity contract consumers will have read into it, not merely a config file. The preceding checkpoint is the gate.</reversibility>
  <verify>
    <automated>node -e '
const fs = require("fs");
const c = JSON.parse(fs.readFileSync(".changeset/config.json", "utf8"));
const g = c.fixed;
if (!Array.isArray(g) || g.length !== 1 || !Array.isArray(g[0])) {
  console.error("fixed must be an array containing exactly one array (nested group), got:", JSON.stringify(g));
  process.exit(1);
}
const want = ["@qadi/core","@qadi/testing","@qadi/promise","@qadi/react","@qadi/http","@qadi/devtools","@qadi/audit","@qadi/predicate-sql","@qadi/predicate-prisma"].sort();
const got = [...g[0]].sort();
if (JSON.stringify(want) !== JSON.stringify(got)) {
  console.error("group membership mismatch.\n  want:", want.join(", "), "\n  got: ", got.join(", "));
  process.exit(1);
}
if (c.access !== "public") { console.error("access must remain public, got", c.access); process.exit(1); }
if (c.changelog !== "@changesets/cli/changelog") { console.error("changelog generator changed:", c.changelog); process.exit(1); }
if (c.commit !== false || c.updateInternalDependencies !== "patch") { console.error("unrelated config field changed"); process.exit(1); }
// every named package must actually exist under packages/ with that exact name
const names = new Set(fs.readdirSync("packages")
  .filter((d) => fs.existsSync(`packages/${d}/package.json`))
  .map((d) => JSON.parse(fs.readFileSync(`packages/${d}/package.json`, "utf8")).name));
const unknown = g[0].filter((n) => !names.has(n));
if (unknown.length) { console.error("group names no real package:", unknown.join(", ")); process.exit(1); }
console.log("fixed group OK: 9 packages, all resolvable, access public");
'</automated>
  </verify>
  <acceptance_criteria>
    - The node assertion above exits 0.
    - `fixed` is an array of length 1 whose single element is an array of length 9.
    - Every one of the nine names resolves to a real `packages/*/package.json` `name` field.
    - `access` still reads `public`; `changelog`, `commit`, `linked`, `baseBranch`, `updateInternalDependencies` and `ignore` are unchanged.
    - `git diff .changeset/config.json` shows changes only within the `fixed` value.
    - No `packages/*/package.json` has been touched yet by this task.
  </acceptance_criteria>
  <done>Every future `changeset version` run in this repository bumps all nine public packages to one shared number.</done>
</task>

<task type="auto">
  <name>Task 3: Run the version bump and carry the private root with it</name>
  <files>packages/audit/package.json, packages/core/package.json, packages/devtools/package.json, packages/http/package.json, packages/predicate-prisma/package.json, packages/predicate-sql/package.json, packages/promise/package.json, packages/react/package.json, packages/testing/package.json, packages/audit/CHANGELOG.md, packages/core/CHANGELOG.md, packages/devtools/CHANGELOG.md, packages/http/CHANGELOG.md, packages/predicate-prisma/CHANGELOG.md, packages/predicate-sql/CHANGELOG.md, packages/promise/CHANGELOG.md, packages/react/CHANGELOG.md, packages/testing/CHANGELOG.md, package.json</files>
  <read_first>
    - `package.json` (root) — the `version` field currently reading `0.0.0`, the `private: true` flag, and the `changeset-version` script. This file is the one `changeset version` will not touch.
    - `.changeset/config.json` — as edited by Task 2, to confirm the group is in place before running the tool.
    - `.planning/phases/01-release-readiness-runtime-verification/01-RESEARCH.md` — Pitfall 1 (`@changesets/cli@3.0.1` excludes Node 20 in its own `engines`, so this step must not run under the floor runtime), Pitfall 3 (`changeset version` will not touch the private root, so D-12 is a separate manual edit and a clean `git diff package.json` after the run is expected, not a tool failure), and Pitfall 4 (the CHANGELOG output is a draft).
    - `.planning/phases/01-release-readiness-runtime-verification/01-02-SUMMARY.md` — the `pnpm changeset status` output recorded there, to compare against.
  </read_first>
  <action>
Run the bump and then close the one gap the tool leaves.

First, per D-09, run `pnpm changeset version` from the repository root. This
consumes every `.changeset/*.md` file, writes the computed version into each of
the nine `packages/*/package.json` manifests, and creates a `CHANGELOG.md` per
package — the first any package in this repo has had.

Do not hand-write any version number. If the tool computes something other than
`0.3.0`, stop and report it rather than editing the result: that outcome is
information about what the recorded changesets say, and ADR-QD-038 exists so
that this arithmetic is never reconstructed by a person. The preceding
checkpoint is where a disagreement gets resolved.

Second, per D-12, edit the root `package.json` `version` from `0.0.0` to
`0.3.0` by hand. This is a separate step, not a consequence of the tool run.
The root is `private: true` and is deliberately not in the fixed group —
changesets skips private packages in every changeset-aware operation, so
`git diff package.json` will show nothing after the version command. That is
expected behaviour, not a failure, and the most likely way D-12 goes silently
unmet is assuming it fell out of the tool. Change only the `version` field;
leave `name`, `private`, `packageManager`, `engines`, `scripts` and every other
field untouched.

Third, confirm what the fixed group actually did, since this is the first time
it has run:
  - `@qadi/audit` received two separate `minor` changesets. Confirm they merged
    into one bump — the package is at `0.3.0`, not bumped twice — and that both
    bodies are present in `packages/audit/CHANGELOG.md`. This is the adjacency
    case: two records at identical severity for one package collapse to one
    version step without either being lost.
  - `@qadi/predicate-sql` and `@qadi/predicate-prisma` each carry one direct
    changeset, and `@qadi/promise` carries one at `patch` severity. All three
    must still land at `0.3.0`, carried by the group rather than by their own
    severity. This is the empty-input case: the fixed group has no hole for a
    package whose own changes would have computed a smaller bump, or none.

Do not run `pnpm changeset publish`, `pnpm publish` or `npm publish`. Do not
create a git tag. Publishing is Phase 2, it is irreversible against the public
registry, and AGENTS.md §16 independently forbids the `npm` form.

Leave the generated CHANGELOG prose alone in this plan. D-13's polish pass is
plan 01-04's task, and it needs the raw tool output as its starting point.
  </action>
  <precondition>The active Node runtime satisfies `@changesets/cli@3.0.1`'s own `engines` field (`^22.11 || ^24 || >=26`). Under Node 20.19.0 — the floor this phase adds to CI — the changeset binary does not run at all. Confirm with `node --version` before invoking `pnpm changeset version`.</precondition>
  <reversibility rating="costly">Reversible by `git revert` while the bump sits in the working tree, and effectively one-way once Phase 2 tags and publishes — after which nine version numbers and nine changelogs are public record. The checkpoint above is the gate; nothing between here and publish asks again.</reversibility>
  <verify>
    <automated>node -e '
const fs = require("fs");
const EXPECTED = "0.3.0";
const bad = [];
const dirs = fs.readdirSync("packages").sort();
let publicCount = 0;
for (const d of dirs) {
  const p = `packages/${d}/package.json`;
  if (!fs.existsSync(p)) continue;
  const m = JSON.parse(fs.readFileSync(p, "utf8"));
  if (m.private === true) continue;
  publicCount++;
  if (m.version !== EXPECTED) bad.push(`${m.name} is ${m.version}`);
  if (!fs.existsSync(`packages/${d}/CHANGELOG.md`)) bad.push(`${m.name} has no CHANGELOG.md`);
}
if (publicCount !== 9) bad.push(`expected 9 public packages, found ${publicCount}`);
const root = JSON.parse(fs.readFileSync("package.json", "utf8"));
if (root.version !== EXPECTED) bad.push(`root package.json is ${root.version}`);
if (root.private !== true) bad.push("root package.json lost its private flag");
const pending = fs.readdirSync(".changeset").filter((f) => f.endsWith(".md") && f !== "README.md");
if (pending.length) bad.push(`unconsumed changesets remain: ${pending.join(", ")}`);
const auditLog = fs.readFileSync("packages/audit/CHANGELOG.md", "utf8");
if (!/ADR-QD-057/.test(auditLog)) bad.push("audit CHANGELOG missing the signature entry");
if (!/INV-QD-051/.test(auditLog)) bad.push("audit CHANGELOG missing the invariants entry");
if (!/0\.3\.0/.test(auditLog)) bad.push("audit CHANGELOG has no 0.3.0 heading");
if (bad.length) { console.error("version bump incomplete:\n  - " + bad.join("\n  - ")); process.exit(1); }
console.log("9 public packages + root at 0.3.0; 9 CHANGELOGs written; changeset queue empty; both audit entries present");
'</automated>
    <manual>`git log --oneline -1` shows no tag was created, and `git tag --points-at HEAD` is empty — this plan versions, it does not release.</manual>
  </verify>
  <acceptance_criteria>
    - The node assertion above exits 0.
    - `pnpm changeset version` produced the nine manifest edits; `git diff` on those nine files shows only the `version` field changed in each.
    - The root `package.json` diff touches only `version`, and `private: true` survives.
    - `ls .changeset/*.md` returns only `README.md`.
    - `packages/audit/CHANGELOG.md` contains one `0.3.0` section carrying both the ADR-QD-057 entry and the INV-QD-051 entry — one bump, both bodies.
    - `packages/promise/CHANGELOG.md`, `packages/predicate-sql/CHANGELOG.md` and `packages/predicate-prisma/CHANGELOG.md` all show `0.3.0`, demonstrating the group carried packages whose own severity would have computed less.
    - `git tag --points-at HEAD` is empty and no publish command appears in the shell history for this task.
  </acceptance_criteria>
  <done>All nine public packages and the private root read one shared, tool-computed `0.3.0`; twenty-six changesets have become nine changelogs; nothing has been published.</done>
</task>

</tasks>

<edge_coverage_resolutions>
Spec-less probe fallback resolutions for REL-03 owned by this plan.

| Probe row | Status | Verification | Resolution |
|---|---|---|---|
| REL-03 / adjacency — "when two things are exactly equal or just touch, do they merge, collide, or separate?" | resolved | explicit | Two changesets naming `@qadi/audit` at the identical `minor` severity **merge**: one version step to 0.3.0, both bodies preserved in the CHANGELOG. Asserted by Task 3's `<automated>` check, which fails if either the ADR-QD-057 or the INV-QD-051 entry is missing from `packages/audit/CHANGELOG.md`. |
| REL-03 / empty — "what is the result for empty, single-element, or null input?" | resolved | explicit | A package with zero direct changesets, or one whose only changeset is `patch`, is still carried to the group version. `@qadi/promise` (one `patch` entry), `@qadi/predicate-sql` and `@qadi/predicate-prisma` (one entry each) are the live cases; Task 3's acceptance criteria assert all three land at 0.3.0. The fixed group has no empty-input hole by construction — that is what makes it lockstep. |
</edge_coverage_resolutions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> public npm registry | The boundary this plan deliberately stops short of. A version bump makes the tree *publishable*; nothing here makes it published. Crossing it is Phase 2, and it is irreversible. |
| recorded changesets -> computed version numbers | The tool is trusted to do the semver arithmetic. The threat is a human overriding its output, which reintroduces exactly the hand-tracked version math ADR-QD-038 removed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Tampering | an unintended `changeset publish` / `pnpm publish` during this plan | high | mitigate | Publishing nine packages to the public npm registry cannot be undone — an unpublish window is narrow and a consumed version number is never reusable. Mitigated by: a `blocking-human` checkpoint before any version write; an explicit plan-level prohibition naming all three publish command forms; Task 3's action forbidding tags; and Task 3's acceptance criterion asserting `git tag --points-at HEAD` is empty. AGENTS.md §16 independently forbids the `npm publish` form in any phase. |
| T-01-09 | Tampering | `.changeset/config.json` `fixed` group membership | medium | mitigate | A misspelled or omitted package name silently drops that package from lockstep, and it ships at a divergent version with no warning. Task 2's `<automated>` check resolves every name in the group against a real `packages/*/package.json` `name` field and fails on any that does not exist, and separately fails if the set differs from the nine. |
| T-01-10 | Tampering | hand-edited version numbers overriding the tool | medium | mitigate | Forbidden by this plan's prohibitions, and structurally discouraged: Task 3's acceptance criteria require the nine manifest diffs to show only the `version` field, which a hand-edit would also satisfy — so the real control is the checkpoint, where the computed number is confirmed against D-11's arithmetic before the write, and the instruction to stop and report rather than edit if they disagree. |
| T-01-11 | Elevation of privilege | `pnpm changeset version` execution | low | accept | Runs a pre-existing devDependency locally against files already in the tree; no network, no credential, no registry contact. |
| T-01-SC | Tampering | npm / pnpm package installs | low | accept | This plan installs no package and adds no dependency. `@changesets/cli@3.0.1` is pre-existing; RESEARCH.md records the Package Legitimacy Audit as not applicable to this phase for that reason. No `[ASSUMED]` or `[SUS]` package enters the tree. |
</threat_model>

<verification>
1. `fixed` is one nested group of exactly nine names, all resolvable to real packages; `access` unchanged.
2. Nine public manifests and the private root all read `0.3.0`, computed not hand-written.
3. Nine `CHANGELOG.md` files exist where none did before.
4. `.changeset/` holds only `README.md` and `config.json`.
5. `packages/audit/CHANGELOG.md` carries both entries under one `0.3.0` heading.
6. No tag, no publish.
</verification>

<success_criteria>
- The lockstep group is configured permanently and correctly shaped.
- One shared version, computed by the tool from the recorded changesets.
- The private root carried along by the manual edit D-12 requires.
- The adjacency and empty cases both demonstrated on real packages rather than reasoned about.
- Nothing published, nothing tagged.
</success_criteria>

<output>
Create `.planning/phases/01-release-readiness-runtime-verification/01-03-SUMMARY.md` when done.
</output>
