---
phase: 01-release-readiness-runtime-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .changeset/audit-signature-harmonization.md
  - .changeset/audit-invariants-mutation-gate.md
autonomous: true
requirements: [REL-03]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`@qadi/audit` carries exactly two new changesets, written as two separate files rather than one combined file, each declaring `\"@qadi/audit\": minor` and naming no other package (D-01, D-02)."
    - "The signature-harmonization changeset marks its consumer-visible break inline in its prose and cites ADR-QD-057, following the callout convention `.changeset/brave-guards-decide.md` already established for a breaking change kept at minor pre-1.0 (D-02, D-03)."
    - "The invariants changeset carries no break callout — the commit it records touched no file under `packages/audit/src/` — and cites the invariant and behavior ids it introduced (D-01)."
    - "`pnpm changeset status` reports `@qadi/audit` under `minor`. Before this plan it appeared under `patch`, which was an internal-dependency bump inherited from `@qadi/core`, not a record of its own two unreleased changes (REL-03)."
    - "Every one of the nine public packages is named directly in at least one `.changeset/*.md` frontmatter block, so REL-03's \"carries a changeset\" holds package by package and not only in aggregate."
  artifacts:
    - .changeset/audit-signature-harmonization.md
    - .changeset/audit-invariants-mutation-gate.md
  key_links:
    - "`.changeset/*.md` frontmatter package names -> `packages/*/package.json` `name` fields. A misspelled scope makes `changeset version` either error or silently skip the package, and REL-03 fails while the file looks correct."
    - "The signature changeset's prose -> `packages/audit/src/SignatureCapturePort.ts`'s actual current exports. The changeset is the only thing an `@qadi/audit` consumer will read about this break; a description that does not match the shipped file is worse than none."
  prohibitions:
    - "Neither changeset body may be a restatement of its source commit message. D-04 is explicit: the prose is written for an `@qadi/audit` consumer deciding what to change in their code, not for a repo historian. A commit message explains a change to the people who made it; a changeset explains it to people who did not."
    - "The signature changeset must not soften or omit the fact that `ElectronicSignature` was removed outright with no compatibility alias. ADR-QD-057 records that as a deliberate decision; a changeset that describes it as a rename or a deprecation would leave a consumer expecting their build to keep compiling."
---

<objective>
Author the two changesets `@qadi/audit` is missing. Twenty-four changeset files
cover eight of the nine public packages; `@qadi/audit` has zero, while carrying
two real unreleased changes. `pnpm changeset status` currently lists it under
`patch` — that is an internal-dependency bump inherited from `@qadi/core`, not
a record of anything `@qadi/audit` itself did.

Purpose: REL-03 requires every public package to carry a changeset recording a
real semver bump. This is the only package where that is untrue, and one of the
two unrecorded changes removed a public type outright.

Output: two `.changeset/*.md` files, both `minor` per this repo's pre-1.0
convention, one of which marks its break inline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-release-readiness-runtime-verification/01-CONTEXT.md
@.planning/phases/01-release-readiness-runtime-verification/01-RESEARCH.md
@.planning/phases/01-release-readiness-runtime-verification/01-PATTERNS.md
@AGENTS.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Created or Modified |
|---|---|---|
| `.changeset/audit-signature-harmonization.md` | changeset file | created |
| `.changeset/audit-invariants-mutation-gate.md` | changeset file | created |

Both files are transient by design: `pnpm changeset version` (plan 01-03)
consumes and deletes them, folding their bodies into
`packages/audit/CHANGELOG.md`. Nothing under `packages/`, `spec/`, `scripts/`
or `.github/` is touched by this plan, and `.changeset/config.json` is
explicitly not touched here — that edit belongs to plan 01-03.

<!-- planner-discipline-allow: **Breaking** -->

<tasks>

<task type="auto">
  <name>Task 1: Changeset for the ElectronicSignature retirement</name>
  <files>.changeset/audit-signature-harmonization.md</files>
  <read_first>
    - `packages/audit/src/SignatureCapturePort.ts` — the file whose public shape changed. Read the module doc comment (its "Harmonized with `@qadi/core`'s `Signature`" paragraph), the `export { SIGNATURE_MEANINGS } from "@qadi/core"` / `export type { SignatureMeaning } from "@qadi/core"` re-export lines, the `SignatureCaptureRequest` interface including its new `signerRole` field and that field's doc comment, and the `SignatureCapturePortShape` interface's `capture` and `validate` signatures. This file, not the commit message, is the source of truth for what a consumer now imports.
    - `spec/decisions/057-audit-signature-harmonization.md` (ADR-QD-057) — the Context and Decision sections. This is the ADR the changeset must cite.
    - `spec/decisions/058-hassignature-a-ninth-service-and-a-decomposable-leaf.md` (ADR-QD-058) — context only, for why `@qadi/core` gained a canonical `Signature` in the first place. Do not cite it in this changeset; it records a `@qadi/core` change, not an `@qadi/audit` one.
    - `.changeset/brave-guards-decide.md` — read in full. It is the precedent for both things this file must do: a genuinely breaking change kept at `minor` pre-1.0, and the inline break-callout style used mid-prose rather than in a separate section.
    - `.changeset/thick-moons-explain.md` — a single-package changeset, for the minimal frontmatter shape.
  </read_first>
  <action>
Create `.changeset/audit-signature-harmonization.md` with frontmatter declaring
`"@qadi/audit": minor` and nothing else — one package, one severity.

`minor` is correct despite this being a break: D-02 records that as this repo's
established pre-1.0 convention, and `.changeset/brave-guards-decide.md` is the
precedent, marking a breaking `DecisionCacheKey.subjectId` -> `.subject` rename
as `minor`.

Write the body fresh, per D-04. Do not adapt the `f86f028` commit message —
that message is written from inside the change, spanning `@qadi/core`,
`@qadi/testing`, `@qadi/http`, `@qadi/devtools` and wayfinder ticket numbers,
almost none of which an `@qadi/audit` consumer needs. Narrow it to what changes
for someone who has `@qadi/audit` in their dependencies.

The body must cover four facts, all verifiable against
`packages/audit/src/SignatureCapturePort.ts`:

  1. `SignatureCapturePort.capture` now returns, and `validate` now accepts,
     `@qadi/core`'s `Signature` type directly. `@qadi/audit`'s own
     `ElectronicSignature` is retired — fully removed, with no compatibility
     type alias. Say why the two types could not both stand: two
     independently-maintained "signature" types, one per package, is the drift
     ADR-QD-002's single-definition reasoning exists to prevent, even though
     the two started out structurally identical. Mark this inline with a
     `**Breaking**:` callout, per D-03, placed the way
     `brave-guards-decide.md` places its callouts — after the paragraph that
     explains the change, not before it.

  2. `SIGNATURE_MEANINGS` and the `SignatureMeaning` type moved to
     `@qadi/core` but are re-exported from `@qadi/audit`, so existing
     `import { SIGNATURE_MEANINGS } from "@qadi/audit"` call sites keep
     working unchanged. Only where the vocabulary is canonically defined
     moved; the value never changed. This is the fact that tells a reader how
     much of their code actually has to change, and it belongs next to the
     break, not in a footnote.

  3. `SignatureCaptureRequest` gains an optional `signerRole`, threaded
     straight into the produced `Signature.signerRole`. A caller with role
     context can populate it; one that omits it gets `undefined`, which is the
     behaviour that existed before the field did — so this is additive and
     needs no action from anyone.

  4. Close with a `See ADR-QD-057.` citation line, matching the closing-citation
     convention every substantial changeset in this directory uses.

Structure it the way the precedents do: an opening framing sentence, then a
bolded lead phrase per distinct point, a paragraph of consumer-facing impact
under each, the inline callout where the break is, then the citation line.
Match their register — these files explain why, not only what, and AGENTS.md's
doc-comment shape rule (summary first, rationale after) is the same instinct
applied to prose.
  </action>
  <verify>
    <automated>python3 -c "
import re
p = '.changeset/audit-signature-harmonization.md'
s = open(p).read()
parts = s.split('---')
fm, body = parts[1], '---'.join(parts[2:]).strip()
assert re.search(r'^\s*[\"\x27]@qadi/audit[\"\x27]\s*:\s*minor\s*\$', fm, re.M), fm
assert len(re.findall(r'@qadi/', fm)) == 1, 'frontmatter must name exactly one package'
assert len(body) >= 500, 'body too thin: %d chars' % len(body)
for token in ['ElectronicSignature', 'Signature', 'SIGNATURE_MEANINGS', 'signerRole', 'ADR-QD-057']:
    assert token in body, 'body must mention ' + token
assert re.search(r'\*\*Break', body), 'body must carry the inline break callout'
print('signature changeset OK (%d chars)' % len(body))
"</automated>
    <manual>Read the body against `packages/audit/src/SignatureCapturePort.ts`. Every claim about what a consumer imports must be checkable against that file's current exports.</manual>
  </verify>
  <acceptance_criteria>
    - The python3 assertion above exits 0.
    - Frontmatter is exactly `"@qadi/audit": minor` and names no second package.
    - The body states that `ElectronicSignature` is removed with no compatibility alias — not renamed, not deprecated.
    - The body states that `SIGNATURE_MEANINGS` / `SignatureMeaning` imports from `@qadi/audit` continue to work.
    - The body describes `signerRole` as optional and additive.
    - The body ends with a `See ADR-QD-057.` citation line.
    - No sentence in the body is a verbatim or near-verbatim lift from `git show f86f028`'s message (D-04).
  </acceptance_criteria>
  <done>An `@qadi/audit` consumer reading only this file knows exactly which of their imports break, which do not, and what new field is available to them.</done>
</task>

<task type="auto">
  <name>Task 2: Changeset for the audit invariants and mutation gate</name>
  <files>.changeset/audit-invariants-mutation-gate.md</files>
  <read_first>
    - `.changeset/bright-roles-explain.md` — read in full. It is the register precedent for a substantial, non-breaking changeset: scene-setting opening, one bolded-lead paragraph per improvement, closing `See BEH-QD-..., INV-QD-...` citation line, no break callout anywhere.
    - `spec/decisions/056-audit-companion-package.md` — the ADR whose two named gaps this commit closed, for context on what "the audit pipeline" is.
    - `stryker.audit.mjs` — the mutation config this commit added, to confirm how it is wired.
    - `package.json` root — the `mutation` script, to confirm `stryker.audit.mjs` is genuinely part of what `pnpm check` runs rather than a standalone config.
    - `.planning/phases/01-release-readiness-runtime-verification/01-RESEARCH.md` — the "Commit 2 — `f0c48fd`" paragraph under Code Examples, which records that this commit's diff touches only `packages/audit/test/*`, `scripts/check-doc-examples.mjs` and `spec/*` — no file under `packages/audit/src/`.
  </read_first>
  <action>
Create `.changeset/audit-invariants-mutation-gate.md` with frontmatter
declaring `"@qadi/audit": minor` and nothing else.

`minor` again, per D-02's blanket pre-1.0 convention, even though this change
has no code-visible API diff. D-01 requires this be a *second, separate* file
rather than folded into Task 1's — the two changes are unrelated and a consumer
scanning a changelog should be able to skip one without skipping the other.

This changeset carries no break callout. The commit it records touched no file
under `packages/audit/src/`; nothing a consumer imports changed. Task 1's file
is where D-03's callout requirement lives, and only there.

Write fresh prose, per D-04, covering:

  - The audit pipeline's correctness guarantees are now formally specified
    rather than implied: INV-QD-051 through INV-QD-055 and BEH-QD-249 through
    BEH-QD-257 name what the package promises. Name the properties concretely
    rather than gesturing at them — staging non-observability, circuit-breaker
    atomicity under concurrent `record()` calls, retention's partition
    property, chain-integrity gap detection, and the signature obligation
    handler's call-once / outcome-match guarantee. A consumer relying on this
    package for an audit trail cares which of those are guaranteed and which
    were previously only intended.
  - A mutation-testing gate (`stryker.audit.mjs`) now runs as part of
    `pnpm check`, so those guarantees are held by tests that are themselves
    checked for whether they would notice a break. Report the score honestly:
    the first run scored 81.46% and revealed real gaps, and it was hardened to
    92.03%, clearing the 90% high threshold. A first-run number reported as
    though it were the final one, or a final number reported without the first,
    both misrepresent what the gate found.
  - Close with a citation line naming the invariant and behavior ranges —
    `See INV-QD-051–055, BEH-QD-249–257.`

Explain why this is worth a release note at all despite having no API diff:
what changed for a consumer is the strength of the guarantee behind an
unchanged interface, and that is exactly the kind of thing a changelog is the
only channel for.
  </action>
  <verify>
    <automated>python3 -c "
import re
p = '.changeset/audit-invariants-mutation-gate.md'
s = open(p).read()
parts = s.split('---')
fm, body = parts[1], '---'.join(parts[2:]).strip()
assert re.search(r'^\s*[\"\x27]@qadi/audit[\"\x27]\s*:\s*minor\s*\$', fm, re.M), fm
assert len(re.findall(r'@qadi/', fm)) == 1, 'frontmatter must name exactly one package'
assert len(body) >= 500, 'body too thin: %d chars' % len(body)
for token in ['INV-QD-051', 'BEH-QD-249', 'stryker.audit.mjs', 'pnpm check']:
    assert token in body, 'body must mention ' + token
assert not re.search(r'\*\*Break', body), 'this changeset must carry no break callout'
print('invariants changeset OK (%d chars)' % len(body))
"</automated>
    <manual>Confirm the invariant and behavior id ranges cited match what `spec/invariants.md` and `spec/behaviors/` actually define for the audit pipeline.</manual>
  </verify>
  <acceptance_criteria>
    - The python3 assertion above exits 0.
    - Frontmatter is exactly `"@qadi/audit": minor`.
    - The body names all five invariant properties, not a summary of them.
    - The body reports both the 81.46% first-run score and the 92.03% hardened score.
    - The body carries no break callout of any form.
    - The body ends with a `See INV-QD-051–055, BEH-QD-249–257.` citation line.
    - This is a second file; Task 1's file is unmodified (`git diff .changeset/audit-signature-harmonization.md` is empty).
  </acceptance_criteria>
  <done>The audit pipeline's newly-formalized guarantees and its mutation gate are recorded as a release-visible change, separately from the signature break.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm REL-03 coverage now holds package by package</name>
  <files>.changeset/audit-signature-harmonization.md, .changeset/audit-invariants-mutation-gate.md</files>
  <read_first>
    - `.changeset/config.json` — to confirm it is still unmodified by this plan. The `fixed` edit belongs to plan 01-03 and must not happen here.
    - `.planning/REQUIREMENTS.md` §Release Readiness — REL-03's exact wording, which is per-package, not aggregate.
    - `.planning/phases/01-release-readiness-runtime-verification/01-RESEARCH.md` — Pitfall 1: `@changesets/cli@3.0.1` declares `engines.node` of `^22.11 || ^24 || >=26` and cannot run on Node 20 at all. `changeset status` is a changesets command and inherits that floor.
  </read_first>
  <action>
Verify, rather than assume, that REL-03's per-package claim now holds.

Run `pnpm changeset status` and confirm `@qadi/audit` has moved from the
`patch` group to the `minor` group. Before this plan it sat under `patch`
alongside `@qadi/promise`; that placement came from `updateInternalDependencies:
"patch"` propagating `@qadi/core`'s bump, and would have carried `@qadi/audit`
into a release with its two real changes unrecorded in any changelog.

Then confirm every one of the nine public packages is named directly in at
least one changeset frontmatter block — the aggregate count of changeset files
does not establish this, since a package can be bumped purely by dependency
propagation. Note that `@qadi/promise` is covered by exactly one direct entry,
at `patch` severity, inside `.changeset/quiet-gates-report.md`; that is a real
recorded bump and satisfies REL-03, so do not add anything for it.

Record both results in the plan summary. Do not run `pnpm changeset version`
here — that is plan 01-03's task, and it must not run before the `fixed` group
is configured.
  </action>
  <precondition>The active Node runtime satisfies `@changesets/cli@3.0.1`'s own `engines` field (`^22.11 || ^24 || >=26`). Under Node 20.19.0 — the floor this phase adds to CI — the `changeset` binary does not run. Check with `node --version` before invoking it.</precondition>
  <verify>
    <automated>python3 -c "
import glob, re, sys, os
want = ['@qadi/core','@qadi/testing','@qadi/promise','@qadi/react','@qadi/http',
        '@qadi/devtools','@qadi/audit','@qadi/predicate-sql','@qadi/predicate-prisma']
direct = {p: 0 for p in want}
for f in glob.glob('.changeset/*.md'):
    if os.path.basename(f) == 'README.md':
        continue
    parts = open(f).read().split('---')
    if len(parts) < 3:
        continue
    for p in want:
        if re.search(r'[\"\x27]%s[\"\x27]\s*:' % re.escape(p), parts[1]):
            direct[p] += 1
missing = [p for p, n in direct.items() if n == 0]
assert not missing, 'no direct changeset for: %s' % ', '.join(missing)
assert direct['@qadi/audit'] == 2, '@qadi/audit should have exactly 2 direct changesets, has %d' % direct['@qadi/audit']
print('all 9 public packages directly covered; @qadi/audit has 2')
" && pnpm changeset status</automated>
    <manual>Read `pnpm changeset status` output: `@qadi/audit` must appear under `minor`. If it still appears under `patch`, one of the two frontmatter blocks is malformed or names the wrong package.</manual>
  </verify>
  <acceptance_criteria>
    - The python3 assertion above exits 0: all nine public packages have at least one direct changeset, and `@qadi/audit` has exactly two.
    - `pnpm changeset status` lists `@qadi/audit` under `minor`.
    - `git status --porcelain .changeset/config.json` is empty — the config edit belongs to plan 01-03.
    - `git status --porcelain packages/` is empty — this plan touches no package manifest.
    - The plan summary records the `changeset status` output verbatim, so plan 01-03 can compare against it after the `fixed` edit.
  </acceptance_criteria>
  <done>REL-03's per-package claim is demonstrated rather than assumed: nine packages, nine direct changesets or more, and `@qadi/audit` recorded at the severity its own changes warrant.</done>
</task>

</tasks>

<edge_coverage_resolutions>
Spec-less probe fallback resolutions for REL-03 that this plan owns. The
remaining REL-03 rows (adjacency, empty) are owned by plan 01-03, where
`changeset version` actually computes the bump; the ordering row is owned by
plan 01-04, where the CHANGELOG is polished.

| Probe row | Status | Verification | Resolution |
|---|---|---|---|
| REL-03 / adjacency — "when two things are exactly equal, do they merge, collide, or separate?" | resolved | explicit | Two changesets naming the same package at the same severity is exactly the "exactly equal" case. They merge into one bump with both bodies preserved. Asserted in plan 01-03's must_haves; this plan's Task 3 establishes the precondition (`@qadi/audit` reported once, under `minor`, not twice). |
</edge_coverage_resolutions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| changeset prose -> published CHANGELOG -> consumer | These two files are consumed by `changeset version` and become the only account a downstream consumer gets of a removed public type. The boundary crossed is informational, and it is one-way: once published, a changelog that understated a break has already misled whoever read it. |
| repository -> public npm registry | **Not crossed by this plan.** No package.json version is touched, no publish command runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Repudiation | `.changeset/audit-signature-harmonization.md` | medium | mitigate | A changelog that understates a removed public type leaves consumers with no record that their build was expected to break, and the repository with no honest account of when it happened. Task 1's `<automated>` check requires the break callout and the `ElectronicSignature` mention to be present; its acceptance criteria require the removal be described as removal, not as a rename or deprecation; and this plan's prohibitions forbid softening it. |
| T-01-07 | Tampering | changeset frontmatter package names | low | mitigate | A misspelled scope silently drops the package from the version run. Task 3's assertion parses every frontmatter block and fails if any of the nine public packages has zero direct entries or if `@qadi/audit` has other than exactly two. |
| T-01-SC | Tampering | npm / pnpm package installs | low | accept | This plan installs no package and adds no dependency. `@changesets/cli` is a pre-existing devDependency whose legitimacy was established at adoption (ADR-QD-038); RESEARCH.md records the Package Legitimacy Audit as not applicable to this phase for that reason. |
</threat_model>

<verification>
1. Both changeset files parse, carry `"@qadi/audit": minor`, and name exactly one package each.
2. The signature changeset carries the inline break callout; the invariants changeset carries none.
3. All nine public packages have at least one direct changeset entry.
4. `pnpm changeset status` reports `@qadi/audit` under `minor`.
5. `.changeset/config.json` and everything under `packages/` are untouched.
</verification>

<success_criteria>
- Two separate changeset files exist for `@qadi/audit`, both `minor`.
- The break is marked inline, cites ADR-QD-057, and is described as a removal.
- The invariants changeset names all five properties and both mutation scores, with no break callout.
- Neither body restates its source commit message.
- REL-03 holds package by package across all nine public packages.
</success_criteria>

<output>
Create `.planning/phases/01-release-readiness-runtime-verification/01-02-SUMMARY.md` when done.
</output>
