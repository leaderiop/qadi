{
  "name": "@qadi/features",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Cucumber BDD acceptance tests for @qadi/core",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b tsconfig.json && tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@qadi/core": "workspace:*",
    "@qadi/devtools": "workspace:*",
    "@qadi/react": "workspace:*",
    "@qadi/testing": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect-cucumber/gherkin": "^0.9.0",
    "@effect-cucumber/vitest": "^0.9.0",
    "@effect/platform-node": "catalog:",
    "@effect/vitest": "catalog:",
    "@happy-dom/global-registrator": "^20.11.1",
    "@testing-library/react": "catalog:",
    "@types/node": "^22.12.0",
    "@types/react": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
