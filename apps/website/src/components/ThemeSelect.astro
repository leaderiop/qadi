---
// Qadi's docs are dark-only, matching the marketing pages, which never had a
// light variant to begin with — there is nothing for a theme toggle to
// switch to, so it's removed rather than left offering an undesigned mode.
---
