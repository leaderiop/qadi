{
  "name": "qadi-monorepo",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "author": "Mohammad AL Mechkor",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/leaderiop/qadi.git"
  },
  "packageManager": "pnpm@10.17.1",
  "description": "Effect-native authorization: permission tokens, role DAG, schema-derived policy ADT, Effect evaluator",
  "scripts": {
    "prepare": "effect-tsgo patch",
    "build": "tsc -b tsconfig.build.json",
    "changeset": "changeset",
    "changeset-version": "changeset version",
    "changeset-publish": "changeset publish",
    "clean": "tsc -b --clean && rm -rf packages/*/lib",
    "typecheck": "tsc -b tsconfig.json && tsc -p tsconfig.test.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:tstyche": "tstyche",
    "coverage": "vitest run --coverage",
    "bench": "vitest bench --run",
    "mutation": "stryker run && stryker run stryker.devtools.mjs && stryker run stryker.predicate-sql.mjs && stryker run stryker.predicate-prisma.mjs && stryker run stryker.audit.mjs && stryker run stryker.http.mjs",
    "test:bdd": "pnpm --filter @qadi/features test",
    "circular": "madge --circular --extensions ts,tsx packages/*/src",
    "lint": "oxlint && node scripts/check-house-style.mjs",
    "lint:fix": "oxlint --fix",
    "lint:style": "node scripts/check-house-style.mjs",
    "format": "oxfmt packages features",
    "format:check": "oxfmt --check packages features",
    "spec:verify": "bash spec/scripts/verify-traceability.sh",
    "spec:verify:strict": "bash spec/scripts/verify-traceability.sh --strict",
    "check": "pnpm typecheck && pnpm lint && pnpm circular && pnpm test:tstyche && pnpm coverage && pnpm test:bdd && pnpm spec:examples && pnpm spec:verify:strict && pnpm spec:gates && pnpm spec:claims && pnpm spec:api && pnpm spec:package && pnpm example && pnpm mutation && pnpm spec:website-examples && pnpm website && pnpm spec:publish",
    "spec:examples": "node scripts/check-doc-examples.mjs",
    "spec:website-examples": "node scripts/check-website-doc-examples.mjs",
    "website": "node scripts/check-website-build.mjs",
    "spec:api": "node scripts/check-api-surface.mjs",
    "spec:package": "node scripts/check-package-install.mjs",
    "spec:gates": "node scripts/check-dod-table.mjs",
    "spec:claims": "node scripts/check-devtools-claims.mjs",
    "spec:publish": "node scripts/check-publish-status.mjs",
    "example": "pnpm --filter @qadi/example-nextjs check"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.2",
    "@effect/atom-react": "catalog:",
    "@effect/language-service": "^0.87.2",
    "@effect/tsgo": "0.45.0",
    "@effect/vitest": "catalog:",
    "@stryker-mutator/core": "10.0.0",
    "@stryker-mutator/vitest-runner": "10.0.0",
    "@types/node": "^22.12.0",
    "@types/react": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "effect": "catalog:",
    "madge": "^8.0.0",
    "oxfmt": "^0.67.0",
    "oxlint": "^1.82.0",
    "react": "catalog:",
    "scheduler": "^0.27.0",
    "tstyche": "^7.2.5",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
